# BaseNative — Full API Reference

<!-- GENERATED by scripts/llms-txt.js — do not edit. Run `node scripts/llms-txt.js`. -->

Generated 2026-09-10 from packages/*/package.json, packages/*/src exports, and docs/*.md. Covers all 40 public packages (`@basenative/fonts` and `@basenative/icons` are private and excluded). Undocumented behavior is listed in Gaps, not invented.

## Template Directives

The complete directive syntax. Verified against packages/runtime/src/bind.js, packages/runtime/src/hydrate.js, and packages/server/src/render.js — client (`hydrate()`) and server (`render()`) implementations agree on all of these.

### `@if` / `@else`
```html
<template @if="isLoggedIn()">
  <p>Welcome back!</p>
</template>
<template @else>
  <p>Please log in.</p>
</template>
```

### `@for` / `@empty`
Loop variables: `$index`, `$first`, `$last`, `$even`, `$odd`.
```html
<template @for="item of items(); track item.id">
  <div>{{ item.name }}</div>
</template>
<template @empty>
  <p>No items.</p>
</template>
```

### `@switch` / `@case` / `@default`
```html
<template @switch="status()">
  <template @case="'active'"><span>Active</span></template>
  <template @case="'inactive'"><span>Inactive</span></template>
  <template @default><span>Unknown</span></template>
</template>
```

### Event Binding — `@event`
```html
<button @click="handleClick()">Click</button>
<input @input="updateValue($event.target.value)">
```

### Attribute Binding — `:attr`
```html
<button :disabled="isLoading()">Save</button>
<div :class="isActive() ? 'active' : ''">Content</div>
```

### Text Interpolation — `{{ }}`
```html
<p>Hello, {{ name() }}!</p>
<span>{{ items().length }} items</span>
```

### Hydratable Mode (server)
```js
const html = render(template, ctx, { hydratable: true });
// Output includes <!--bn:if-->...<!--/bn:if--> markers
```

### Server-Side Directives (server)
All template directives (`@if`, `@for`, `@switch`, `:attr`, `{{ }}`) are evaluated at render time. Event handlers (`@click`, `@input`) are stripped from the output (they activate on client hydration).


## Packages

### `@basenative/admin` (v1.0.0)

@basenative/admin — moderation & admin surface.

Source: `packages/admin/src`

- `./migrations/0001` → `./migrations/0001_audit_and_roles.sql` (non-JS resource export)

#### `defineRoles(opts = {})` (function)
Build a role checker from a hierarchy.
packages/admin/src/roles.js · `import { defineRoles } from '@basenative/admin';`

#### `hasRole(user, role, hierarchy = ['user', 'moderator', 'admin'])` (function)
Pure helper — check role against any hierarchy without instantiating.
packages/admin/src/roles.js · `import { hasRole } from '@basenative/admin';`

#### `requireRole(role, opts = {})` (function)
Build a guard that throws (or returns a 403-shaped object) when the supplied user does not satisfy the required role.
packages/admin/src/roles.js · `import { requireRole } from '@basenative/admin';`

#### `roleSeed({ env, envKey = 'ADMIN_HANDLES', user, seedMap, identifier = (u) => u?.handle, targetRole = 'admin', setRole, })` (async function)
Seed an elevated role for a user when a deployment-time allowlist matches.
packages/admin/src/roles.js · `import { roleSeed } from '@basenative/admin';`

#### `defineQueue({ db, tables, now = () => Date.now(), onApprove } = {})` (function)
Build a queue store bound to a DB and a pair of tables.
packages/admin/src/queue.js · `import { defineQueue } from '@basenative/admin';`

#### `auditAction(env, entry)` (async function)
Record an audit entry. All fields except `action` are optional.
packages/admin/src/audit.js · `import { auditAction } from '@basenative/admin';`

#### `AUDIT_MIGRATION` (const)
SQL migration text for the audit_log table. Re-exported from `migrations/0001_audit_and_roles.sql` for convenience when you want to apply it programmatically (e.g. in a worker startup hook).
packages/admin/src/audit.js · `import { AUDIT_MIGRATION } from '@basenative/admin';`

#### `defineAdminHandlers(cfg)` (function)
packages/admin/src/handlers.js · `import { defineAdminHandlers } from '@basenative/admin';`

#### `renderAdminQueueList({ items, approveLabel = 'APPROVE', rejectLabel = 'REJECT', emptyLabel = 'queue empty.', actionHandler = 'bn-admin-decid…)` (function)
Render the moderation queue list.
packages/admin/src/components.js · `import { renderAdminQueueList } from '@basenative/admin';`

#### `renderAdminUserList({ users, results = null, query = '', currentHandle = '', roles = ['user', 'moderator', 'admin'], labels = {}, actionHan…)` (function)
Render the admin user-management list with search and per-row role controls.
packages/admin/src/components.js · `import { renderAdminUserList } from '@basenative/admin';`

#### `withAudit(cfg)` (function)
Wrap a request handler so every successful invocation records an audit. The wrapper resolves `user`, `action`, `target`, and `meta` from the supplied callbacks at call-time so they can read from the request.
packages/admin/src/audit.js · `import { withAudit } from '@basenative/admin';`

#### `onRequestGet` (const)
packages/admin/src/handlers.js · `import { onRequestGet } from '@basenative/admin';`

#### `onRequestPost` (const)
packages/admin/src/handlers.js · `import { onRequestPost } from '@basenative/admin';`

### `@basenative/auth` (v0.3.0)

Authentication, session management, RBAC, and OAuth2/OIDC for BaseNative apps

Source: `packages/auth/src` · Docs: `docs/api/auth.md`

### API Reference

#### createSessionManager(options)
Creates a session manager with configurable storage and cookie settings. **Parameters:** - `options.store` — storage backend; defaults to `createMemoryStore()` - `options.cookieName` — cookie name; default `'bn_session'` - `options.maxAge`…
```js
const sessions = createSessionManager({
  store: createDbStore(adapter),
  maxAge: 7 * 24 * 60 * 60 * 1000, // 7 days
});

const session = await sessions.create({ user: { id: 42, role: 'admin' } });
const loaded = await sessions.get(session.id);
await sessions.destroy(session.id);
```

#### createMemoryStore()
In-memory session store. Suitable for development and testing. **Returns:** Store object with `get`, `set`, `delete`, `clear`, `size`.

#### createDbStore(adapter, options)
Database-backed session store. Requires a table with `id`, `data`, and `expires_at` columns. **Parameters:** - `adapter` — database adapter with `queryOne` and `execute` methods - `options.tableName` — table name; default `'sessions'` **Re…

#### hashPassword(password)
Hashes a password using `node:crypto` (PBKDF2 with SHA-512). No external dependencies. **Parameters:** - `password` — plain text password string **Returns:** `Promise<string>` — encoded hash string

#### verifyPassword(password, hash)
Verifies a plain text password against a stored hash. **Parameters:** - `password` — plain text password to verify - `hash` — hash produced by `hashPassword` **Returns:** `Promise<boolean>`

#### defineRoles(definition)
Defines roles and their permissions, with optional inheritance. **Parameters:** - `definition` — object mapping role names to `{ permissions: string[], inherits?: string[] }` **Returns:** RBAC engine with methods: `can`, `canAll`, `canAny`…
```js
const rbac = defineRoles({
  admin: { permissions: ['*'] },
  editor: { permissions: ['write'], inherits: ['viewer'] },
  viewer: { permissions: ['read'] },
});

rbac.can('editor', 'read');   // true (inherited)
rbac.can('viewer', 'write');  // false
rbac.getPermissions('editor'); // ['write', 'read']
```

#### createGuard(rbac, options)
Creates route guard middleware factories from an RBAC engine. **Parameters:** - `rbac` — RBAC engine from `defineRoles` - `options.getRoleFromContext` — function `(ctx) => string`; default reads `ctx.state.user.role` - `options.onDenied` —…
```js
const guard = createGuard(rbac);

app.get('/admin', guard.requireRole('admin'), handler);
app.post('/posts', guard.require('write'), handler);
```

#### sessionMiddleware(sessionManager)
Loads session from the request cookie and attaches it to `ctx.state`. After this middleware runs: - `ctx.state.session` — session object or `null` - `ctx.state.user` — `session.data.user` or `null` - `ctx.state.isAuthenticated` — boolean

#### requireAuth(options)
Middleware that rejects unauthenticated requests. **Parameters:** - `options.redirectTo` — redirect URL; if omitted returns 401 - `options.message` — body text for 401 responses; default `'Authentication required'`

#### login(sessionManager, ctx, user)
Creates a session for the given user and attaches it to context. **Parameters:** - `sessionManager` — session manager instance - `ctx` — request context - `user` — user object to store in session data **Returns:** `Promise<Session>`

#### logout(sessionManager, ctx)
Destroys the current session and clears the session cookie.

#### credentialsProvider(options)
Email/password authentication provider. **Parameters:** - `options.findUser(email)` — async function returning a user object with `passwordHash` - `options.onSuccess(user, ctx)` — called on successful login - `options.onFailure(ctx)` — cal…

#### oauthProvider(providerName, options)
OAuth 2.0 provider adapter. Built-in provider configs available via `providers`. **Parameters:** - `providerName` — key from the `providers` map (e.g. `'github'`, `'google'`) - `options.clientId` — OAuth app client ID - `options.clientSecr…

### `@basenative/auth-webauthn` (v1.0.2)

WebAuthn (passkey) server adapter for @basenative/auth.

Source: `packages/auth-webauthn/src`

#### `normHandle(h)` (function)
packages/auth-webauthn/src/server.js · `import { normHandle } from '@basenative/auth-webauthn';`

#### `validHandle(h)` (function)
packages/auth-webauthn/src/server.js · `import { validHandle } from '@basenative/auth-webauthn';`

#### `b64uToBytes(s)` (function)
packages/auth-webauthn/src/server.js · `import { b64uToBytes } from '@basenative/auth-webauthn';`

#### `bytesToB64u(bytes)` (function)
packages/auth-webauthn/src/server.js · `import { bytesToB64u } from '@basenative/auth-webauthn';`

#### `userIdBytes(uuid)` (function)
Pack a UUID (with dashes) into 16 raw bytes for WebAuthn userID.
packages/auth-webauthn/src/server.js · `import { userIdBytes } from '@basenative/auth-webauthn';`

#### `getCookie(request, name)` (function)
packages/auth-webauthn/src/server.js · `import { getCookie } from '@basenative/auth-webauthn';`

#### `setCookieHeader(name, value, opts = {})` (function)
packages/auth-webauthn/src/server.js · `import { setCookieHeader } from '@basenative/auth-webauthn';`

#### `clearCookieHeader(name)` (function)
packages/auth-webauthn/src/server.js · `import { clearCookieHeader } from '@basenative/auth-webauthn';`

#### `webauthnAdapter(opts)` (function)
Build a WebAuthn provider/adapter usable with @basenative/auth.
packages/auth-webauthn/src/server.js · `import { webauthnAdapter } from '@basenative/auth-webauthn';`

#### `d1WebAuthnStores(DB)` (function)
Build the four-store object from a Cloudflare D1 binding.
packages/auth-webauthn/src/d1-stores.js · `import { d1WebAuthnStores } from '@basenative/auth-webauthn';`

#### `seedRoles({ stores, user, seedMap, changedBy = 'seed' })` (async function)
Generic role-seed-on-login helper.
packages/auth-webauthn/src/seed-role.js · `import { seedRoles } from '@basenative/auth-webauthn';`

#### `parseHandleList(csv, role)` (function)
Convenience: parse a comma-separated env var list ("alice,bob,charlie") into a `seedMap` for a single role.
packages/auth-webauthn/src/seed-role.js · `import { parseHandleList } from '@basenative/auth-webauthn';`

#### `isPasskeySupported()` (function)
True iff the browser exposes the WebAuthn API.
packages/auth-webauthn/src/client.js · `import { isPasskeySupported } from '@basenative/auth-webauthn';`

#### `isPlatformPasskeySupported()` (async function)
Some browsers (Safari, Firefox-no-PRF) report yes for one path but not platform-bound passkeys. Returns true if a platform authenticator is available and resident-key creation is plausible.
packages/auth-webauthn/src/client.js · `import { isPlatformPasskeySupported } from '@basenative/auth-webauthn';`

#### `registerPasskey(handle, opts = {})` (async function)
Register a new passkey for `handle`.
packages/auth-webauthn/src/client.js · `import { registerPasskey } from '@basenative/auth-webauthn';`

#### `loginPasskey(handle, opts = {})` (async function)
Log in via passkey. `handle` may be empty for usernameless flows (the user picks a passkey from the OS prompt).
packages/auth-webauthn/src/client.js · `import { loginPasskey } from '@basenative/auth-webauthn';`

#### `me(opts = {})` (async function)
Get the current user (or `{ user: null }`).
packages/auth-webauthn/src/client.js · `import { me } from '@basenative/auth-webauthn';`

#### `logout(opts = {})` (async function)
Log out and clear the session cookie.
packages/auth-webauthn/src/client.js · `import { logout } from '@basenative/auth-webauthn';`

#### `registerOptionsHandler(getAdapter)` (function)
packages/auth-webauthn/src/handlers.js · `import { registerOptionsHandler } from '@basenative/auth-webauthn';`

#### `registerVerifyHandler(getAdapter, hooks = {})` (function)
packages/auth-webauthn/src/handlers.js · `import { registerVerifyHandler } from '@basenative/auth-webauthn';`

#### `loginOptionsHandler(getAdapter)` (function)
packages/auth-webauthn/src/handlers.js · `import { loginOptionsHandler } from '@basenative/auth-webauthn';`

#### `loginVerifyHandler(getAdapter, hooks = {})` (function)
packages/auth-webauthn/src/handlers.js · `import { loginVerifyHandler } from '@basenative/auth-webauthn';`

#### `meHandler(getAdapter, options = {})` (function)
packages/auth-webauthn/src/handlers.js · `import { meHandler } from '@basenative/auth-webauthn';`

#### `logoutHandler(getAdapter)` (function)
packages/auth-webauthn/src/handlers.js · `import { logoutHandler } from '@basenative/auth-webauthn';`

#### `onRequestPost` (const)
packages/auth-webauthn/src/handlers.js · `import { onRequestPost } from '@basenative/auth-webauthn';`

#### `d1Users(DB)` (function)
packages/auth-webauthn/src/d1-stores.js · `import { d1Users } from '@basenative/auth-webauthn';`

#### `d1Credentials(DB)` (function)
packages/auth-webauthn/src/d1-stores.js · `import { d1Credentials } from '@basenative/auth-webauthn';`

#### `d1Challenges(DB)` (function)
packages/auth-webauthn/src/d1-stores.js · `import { d1Challenges } from '@basenative/auth-webauthn';`

#### `d1UserSessions(DB)` (function)
packages/auth-webauthn/src/d1-stores.js · `import { d1UserSessions } from '@basenative/auth-webauthn';`

#### `migration` (const)
Cloudflare D1 store factory for @basenative/auth-webauthn.
packages/auth-webauthn/src/d1-stores.js · `import { migration } from '@basenative/auth-webauthn';`

### `@basenative/builder` (v0.1.0)

Signal-based drag-and-drop component builder for BaseNative — composes layouts, generates clean BaseNative code

Source: `packages/builder/src`

- `./builder.css` → `./src/builder.css` (non-JS resource export)

#### `createBuilderState(options = {})` (function)
packages/builder/src/state.js · `import { createBuilderState } from '@basenative/builder';`

#### `createPalette()` (function)
packages/builder/src/palette.js · `import { createPalette } from '@basenative/builder';`

#### `defaultPalette()` (function)
packages/builder/src/palette.js · `import { defaultPalette } from '@basenative/builder';`

#### `generateBaseNative(state, options = {})` (function)
packages/builder/src/codegen.js · `import { generateBaseNative } from '@basenative/builder';`

#### `renderTreeView(state)` (function)
packages/builder/src/tree-view.js · `import { renderTreeView } from '@basenative/builder';`

#### `renderInspector(state, palette)` (function)
packages/builder/src/inspector.js · `import { renderInspector } from '@basenative/builder';`

#### `renderPaletteHTML(palette)` (function)
packages/builder/src/palette-element.js · `import { renderPaletteHTML } from '@basenative/builder';`

#### `escapeHtml(value)` (function)
packages/builder/src/escape.js · `import { escapeHtml } from '@basenative/builder';`

#### `escapeAttr(value)` (function)
packages/builder/src/escape.js · `import { escapeAttr } from '@basenative/builder';`

#### `isValidIdentifier(name)` (function)
packages/builder/src/escape.js · `import { isValidIdentifier } from '@basenative/builder';`

#### `renderNodeToElement(doc, node, palette)` (function)
packages/builder/src/dom-render.js · `import { renderNodeToElement } from '@basenative/builder';`

#### `BnBuilder` (class)
packages/builder/src/builder-element.js

#### `BnBuilderCanvas` (class)
packages/builder/src/canvas-element.js

#### `BnBuilderPalette` (class)
packages/builder/src/palette-element.js

#### `BnBuilderTree` (class)
packages/builder/src/tree-element.js

#### `BnBuilderInspector` (class)
packages/builder/src/inspector-element.js

### `@basenative/claude-config` (v0.2.0)

Bundled Claude Code subagents, skills, slash commands, hooks, and settings for DuganLabs / BaseNative projects. Drop in via `bn-claude install`.

Source: `packages/claude-config/src`

#### `PACKAGE_ROOT` (const)
Absolute path to the package root (one above src/).
packages/claude-config/src/paths.js · `import { PACKAGE_ROOT } from '@basenative/claude-config';`

#### `TEMPLATE_KINDS` (const)
Each template kind we ship — directory under PACKAGE_ROOT, target under .claude/.
packages/claude-config/src/paths.js · `import { TEMPLATE_KINDS } from '@basenative/claude-config';`

#### `SETTINGS_TEMPLATE` (const)
The settings template ships separately from the kinds above.
packages/claude-config/src/paths.js · `import { SETTINGS_TEMPLATE } from '@basenative/claude-config';`

#### `CLAUDE_MD_TEMPLATE` (const)
packages/claude-config/src/paths.js · `import { CLAUDE_MD_TEMPLATE } from '@basenative/claude-config';`

#### `install({ projectRoot, force = false, dryRun = false, quiet = false } = {})` (async function)
Install templates into <projectRoot>/.claude/.
packages/claude-config/src/install.js · `import { install } from '@basenative/claude-config';`

#### `verify({ projectRoot } = {})` (function)
Verify the project has all expected files. Returns { ok, missing, extras }.
packages/claude-config/src/install.js · `import { verify } from '@basenative/claude-config';`

#### `listInstalled({ projectRoot } = {})` (function)
List installed agents/skills/commands/hooks for a project.
packages/claude-config/src/install.js · `import { listInstalled } from '@basenative/claude-config';`

### `@basenative/cli` (v0.4.0)

The bn CLI — front door for BaseNative / DuganLabs projects (create, prd, speckit, gh, nx, deploy, doctor)

Source: `packages/cli/src` · Docs: `docs/api/cli.md`

- `./manifest` → `./manifest.json` (non-JS resource export)

### Commands

#### `bn create <name> [options]`
Scaffolds a new BaseNative project. **Arguments:** - `name` — project directory name **Options:** - `--template <name>` — project template to use (e.g. `enterprise`, `cloudflare-workers`, `node`) **Example:**
```bash
bn create my-saas --template enterprise
cd my-saas
pnpm install
bn dev
```

#### `bn dev [options]`
Starts the development server with hot reload. **Options:** - `--port <number>` — port to listen on; default `3000`

#### `bn build`
Bundles the application for production using ESBuild. Output goes to `./dist`.

#### `bn generate <type> <name>`
Generates scaffolding for components, routes, or pages. **Types:** - `component <name>` — generates a semantic HTML component following the Trinity Standard (state + logic + template in one file) - `route <path>` — generates a route handle…

#### `bn deploy [options]`
Deploys the application to BaseNative Cloud. **Options:** - `--env <name>` — target deployment environment (e.g. `production`, `staging`)

#### `bn env <subcommand>`
Manages environment variables for the project. **Subcommands:** - `bn env set <KEY> <VALUE>` — sets an environment variable - `bn env get <KEY>` — reads a single variable - `bn env list` — lists all configured variables - `bn env delete <K…

#### `bn analyze`
Analyzes bundle size and reports dependency sizes. Useful for verifying that `@basenative/runtime` stays under its 10KB gzipped budget (currently 8.8KB).

#### `bn help`
Prints the help message listing all commands.

#### `bn --version`
Prints the installed CLI version.

### `@basenative/combobox` (v1.0.0)

@basenative/combobox — accessible typeahead combobox primitive.

Source: `packages/combobox/src`

- `./css` → `./src/styles.css` (non-JS resource export)
- `./styles.css` → `./src/styles.css` (non-JS resource export)

#### `Combobox(options = {})` (function)
One-shot convenience. Returns `{ html, hydrate }` so callers can embed the markup in SSR output and wire reactivity later.
packages/combobox/src/combobox.js · `import { Combobox } from '@basenative/combobox';`

#### `renderCombobox(options = {})` (function)
Render the combobox as an HTML string — pre-paint friendly. The static markup includes: - root <div role="combobox" aria-expanded="false"> - <label> (only when `label` provided) - <input role="combobox" aria-autocomplete="list"> - <ul role…
packages/combobox/src/combobox.js · `import { renderCombobox } from '@basenative/combobox';`

#### `hydrateCombobox(rootEl, options = {})` (function)
Hydrate a rendered combobox. `rootEl` is the element produced by `renderCombobox` (or any DOM that follows the data-bn contract).
packages/combobox/src/combobox.js · `import { hydrateCombobox } from '@basenative/combobox';`

#### `normalizeOption(raw)` (function)
Lift a raw option (string OR `{ value, label, hint? }`) into a normalized shape. Strings become `{ value: s, label: s }`.
packages/combobox/src/combobox.js · `import { normalizeOption } from '@basenative/combobox';`

#### `defaultFilter(option, query)` (function)
Case-insensitive substring match against label + value + hint. Empty queries match everything (so the listbox shows all options on focus).
packages/combobox/src/filter.js · `import { defaultFilter } from '@basenative/combobox';`

#### `prefixFilter(option, query)` (function)
Case-insensitive prefix match against label only. Best for short option lists where you want predictable typeahead ordering.
packages/combobox/src/filter.js · `import { prefixFilter } from '@basenative/combobox';`

#### `fuzzyFilter(option, query)` (function)
Lightweight in-order subsequence matcher — every query character must appear (in order, case-insensitive) somewhere in the label. No scoring; preserves caller-provided ordering.
packages/combobox/src/filter.js · `import { fuzzyFilter } from '@basenative/combobox';`

#### `applyAriaAttributes(rootEl, options = {})` (function)
Idempotently apply ARIA wiring to a combobox root + input + listbox. Safe to call after re-render or on hydrate.
packages/combobox/src/a11y.js · `import { applyAriaAttributes } from '@basenative/combobox';`

#### `escapeOnKeydown(rootEl, onEscape)` (function)
Bind Escape to a handler at the document level scoped to this root — closes the popup from anywhere inside the widget. Returns an unbind fn.
packages/combobox/src/a11y.js · `import { escapeOnKeydown } from '@basenative/combobox';`

#### `announce(rootEl, message)` (function)
Announce a transient message to screen readers via the widget's aria-live region. Used for "N options available" / "Press Enter to create '...'" style hints. Idempotent — safe to call repeatedly.
packages/combobox/src/a11y.js · `import { announce } from '@basenative/combobox';`

### `@basenative/components` (v0.4.0)

15 semantic UI components with CSS custom property theming and density scales

Source: `packages/components/src` · Docs: `docs/api/components.md`

- `./css` → `./src/index.css` (non-JS resource export)
- `./tokens.css` → `./src/tokens.css` (non-JS resource export)
- `./theme.css` → `./src/theme.css` (non-JS resource export)
- `./components.css` → `./src/components.css` (non-JS resource export)
- `./layout.css` → `./src/layout.css` (non-JS resource export)
- `./reset.css` → `./src/reset.css` (non-JS resource export)
- `./states.css` → `./src/states.css` (non-JS resource export)

### Button
Variants: `primary`, `secondary`, `destructive`, `ghost`. Sizes: `default`, `sm`, `lg`.
```js
renderButton('Submit', { variant: 'primary', disabled: false })
```

### Input
```js
renderInput({ name: 'email', label: 'Email', type: 'email', error: 'Required' })
```

### Textarea
```js
renderTextarea({ name: 'bio', label: 'Bio', rows: 5 })
```

### Checkbox
```js
renderCheckbox({ name: 'agree', label: 'I agree to terms', checked: false })
```

### Radio Group
```js
renderRadioGroup({
  name: 'plan',
  label: 'Plan',
  items: [{ value: 'free', label: 'Free' }, { value: 'pro', label: 'Pro' }],
  selected: 'free',
})
```

### Toggle / Switch
```js
renderToggle({ name: 'notifications', label: 'Enable notifications' })
```

### Select
```js
renderSelect({
  name: 'country',
  label: 'Country',
  placeholder: 'Select...',
  items: [{ value: 'us', label: 'USA' }, { value: 'uk', label: 'UK' }],
})
```

### Alert
Variants: `info`, `success`, `warning`, `error`.
```js
renderAlert('Changes saved!', { variant: 'success', dismissible: true })
```

### Toast
Server-side: `renderToastContainer('top-right')`
```js
import { createToaster, showToast, dismissToast } from '@basenative/components';

const toaster = createToaster({ position: 'top-right', duration: 5000 });
showToast(toaster, { message: 'Saved!', variant: 'success' });
```

### Table
```js
renderTable({
  columns: [{ key: 'name', label: 'Name', sortable: true }],
  rows: [{ name: 'Alice' }],
  emptyMessage: 'No data',
  caption: 'Users',
})
```

### Pagination
```js
renderPagination({ currentPage: 2, totalPages: 10, baseUrl: '/users' })
```

### Badge
Variants: `default`, `primary`, `success`, `warning`, `error`.
```js
renderBadge('Active', { variant: 'success' })
```

### Card
```js
renderCard({ header: 'Title', body: '<p>Content</p>', footer: 'Footer' })
```

### Progress
```js
renderProgress({ value: 75, max: 100, label: 'Upload progress' })
```

### Spinner
Sizes: `sm`, `default`, `lg`.
```js
renderSpinner({ size: 'lg', label: 'Loading data' })
```

### Skeleton
Variants: `text`, `circle`.
```js
renderSkeleton({ width: '200px', height: '1rem', count: 3 })
```

#### Undocumented exports (source-only — not in docs/api/components.md)

#### `buttonVariants(variant = 'primary', size = 'default')` (function)
Returns the CSS class string for a button variant.
packages/components/src/button.js · `import { buttonVariants } from '@basenative/components';`

#### `renderCombobox(options = {})` (function)
Combobox — searchable dropdown with keyboard navigation. Uses native <input> + <datalist> pattern with progressive enhancement.
packages/components/src/combobox.js · `import { renderCombobox } from '@basenative/components';`

#### `renderMultiselect(options = {})` (function)
Multiselect — multiple value selection with tags/chips.
packages/components/src/multiselect.js · `import { renderMultiselect } from '@basenative/components';`

#### `renderDataGrid(options = {})` (function)
Data grid — sortable, filterable, paginated table with virtual scrolling support.
packages/components/src/datagrid.js · `import { renderDataGrid } from '@basenative/components';`

#### `renderTree(options = {})` (function)
Tree view — expandable hierarchical tree.
packages/components/src/tree.js · `import { renderTree } from '@basenative/components';`

#### `renderTreeGrid(options = {})` (function)
TreeGrid — tree structure combined with table columns.
packages/components/src/tree.js · `import { renderTreeGrid } from '@basenative/components';`

#### `renderVirtualList(options = {})` (function)
Virtualizer — renders a virtual scroll container placeholder. Actual virtualization happens client-side via hydration. Server renders a window of items + a spacer for total height.
packages/components/src/virtualizer.js · `import { renderVirtualList } from '@basenative/components';`

#### `renderDialog(options = {})` (function)
Dialog — modal/non-modal dialog using native <dialog> element.
packages/components/src/dialog.js · `import { renderDialog } from '@basenative/components';`

#### `renderDrawer(options = {})` (function)
Drawer — side panel that slides in from the edge.
packages/components/src/drawer.js · `import { renderDrawer } from '@basenative/components';`

#### `renderTabs(options = {})` (function)
Tabs — tab navigation with panels.
packages/components/src/tabs.js · `import { renderTabs } from '@basenative/components';`

#### `renderAccordion(options = {})` (function)
Accordion — collapsible sections using native <details>/<summary>.
packages/components/src/accordion.js · `import { renderAccordion } from '@basenative/components';`

#### `renderBreadcrumb(options = {})` (function)
Breadcrumb — navigation trail.
packages/components/src/breadcrumb.js · `import { renderBreadcrumb } from '@basenative/components';`

#### `renderAvatar(options = {})` (function)
Avatar — user avatar with image, initials, or icon fallback.
packages/components/src/avatar.js · `import { renderAvatar } from '@basenative/components';`

#### `renderTooltip(options = {})` (function)
Tooltip — popover-based tooltip using the Popover API.
packages/components/src/tooltip.js · `import { renderTooltip } from '@basenative/components';`

#### `renderDropdownMenu(options = {})` (function)
Dropdown menu — popover-based menu.
packages/components/src/dropdown-menu.js · `import { renderDropdownMenu } from '@basenative/components';`

#### `renderCommandPalette(options = {})` (function)
Command palette — Cmd+K style searchable command menu.
packages/components/src/command-palette.js · `import { renderCommandPalette } from '@basenative/components';`

#### `renderCalendar(options = {})` (function)
Server-side render a weekly calendar grid with event blocks.
packages/components/src/calendar.js · `import { renderCalendar } from '@basenative/components';`

#### `renderPipelineBlock(options = {})` (function)
Render a pipeline/kanban block for use outside the calendar (e.g., sidebar cards that can be dragged onto the calendar).
packages/components/src/calendar.js · `import { renderPipelineBlock } from '@basenative/components';`

#### `renderPipeline(options = {})` (function)
Render a kanban-style pipeline view with draggable columns and cards.
packages/components/src/calendar.js · `import { renderPipeline } from '@basenative/components';`

#### `initCalendarDragDrop(container, callbacks = {})` (function)
Client-side: Initialize drag-and-drop on a calendar container.
packages/components/src/calendar.js · `import { initCalendarDragDrop } from '@basenative/components';`

#### `initPipelineDragDrop(container, callbacks = {})` (function)
Client-side: Initialize drag-and-drop on a pipeline container.
packages/components/src/calendar.js · `import { initPipelineDragDrop } from '@basenative/components';`

#### `createCalendarState(options = {})` (function)
Create a calendar state container with signal-based reactive events.
packages/components/src/calendar-state.js · `import { createCalendarState } from '@basenative/components';`

#### `createPipelineState(options = {})` (function)
Create a pipeline state container for kanban-style workflow.
packages/components/src/calendar-state.js · `import { createPipelineState } from '@basenative/components';`

#### `eventsCollide(event1, event2)` (function)
Check if two events collide in the calendar.
packages/components/src/calendar-state.js · `import { eventsCollide } from '@basenative/components';`

#### `updateEvent(event, overrides)` (function)
Create a new event object with merged properties.
packages/components/src/calendar-state.js · `import { updateEvent } from '@basenative/components';`

#### `getEventDuration(start, end)` (function)
Calculate duration in minutes between ISO datetime strings.
packages/components/src/calendar-state.js · `import { getEventDuration } from '@basenative/components';`

#### `renderLayoutGrid(options = {})` (function)
renderLayoutGrid - A drag-and-drop CSS grid layout component. Designed to hook into @basenative/visual-builder's canvas state.
packages/components/src/layout-grid.js · `import { renderLayoutGrid } from '@basenative/components';`

#### `layoutGridStyles()` (function)
CSS for layout grid components.
packages/components/src/layout-grid.js · `import { layoutGridStyles } from '@basenative/components';`

### `@basenative/config` (v0.3.0)

Type-safe environment configuration loading and validation

Source: `packages/config/src` · Docs: `docs/api/config.md`

### API Reference

#### defineConfig(options)
Loads and validates configuration from environment variables. **Parameters:** - `options.schema` — schema definition: either a key-to-validator map or a function (from `zodAdapter`) - `options.env` — source of environment variables; defaul…
```js
// Reads APP_PORT, APP_DATABASE_URL from process.env
const config = defineConfig({
  prefix: 'APP_',
  schema: {
    port: number(),
    database_url: string(),
  },
});
```

#### string(options)
Validator that expects a non-empty string. **Parameters:** - `options.minLength` — minimum string length - `options.maxLength` — maximum string length **Returns:** Validator function.

#### number(options)
Validator that coerces the value to a number. **Parameters:** - `options.min` — minimum value (inclusive) - `options.max` — maximum value (inclusive) **Returns:** Validator function. The parsed value in the config object will be a `number`.

#### boolean()
Validator that accepts `'true'`, `'1'`, `true`, `'false'`, `'0'`, or `false`. **Returns:** Validator function. The parsed value will be a `boolean`.

#### oneOf(allowed)
Validator that enforces the value is one of the given strings. **Parameters:** - `allowed` — array of allowed string values **Returns:** Validator function. **Example:**
```js
NODE_ENV: oneOf(['development', 'production']),
```

#### optional(validator, defaultValue)
Wraps any validator to make the key optional. Returns `defaultValue` when the variable is absent or empty. **Parameters:** - `validator` — any validator function - `defaultValue` — value to use when the variable is absent **Returns:** Vali…

#### validateConfig(values, schema)
Validates a plain object against a schema map. Throws on any failures. **Parameters:** - `values` — object of raw string values - `schema` — key-to-validator map **Returns:** Validated config object.

#### zodAdapter(zodSchema)
Wraps a Zod schema for use as the `schema` option in `defineConfig`. **Parameters:** - `zodSchema` — a Zod schema object (any schema with `.safeParse`) **Returns:** Function compatible with `defineConfig`'s `schema` option. **Example:**
```js
import { z } from 'zod';
import { defineConfig, zodAdapter } from '@basenative/config';

const config = defineConfig({
  schema: zodAdapter(z.object({
    PORT: z.coerce.number().default(3000),
    DATABASE_URL: z.string().url(),
  })),
});
```

#### loadEnv(path)
Loads a `.env` file and merges it into `process.env`. Variables already set in the environment are not overwritten. **Parameters:** - `path` — path to the `.env` file; default `'.env'`

#### parseEnvFile(content)
Parses `.env` file content into a plain object without modifying `process.env`. **Parameters:** - `content` — string content of a `.env` file **Returns:** `Record<string, string>`

### `@basenative/date` (v0.3.0)

Date utilities, timezone handling, relative time formatting, and date ranges

Source: `packages/date/src` · Docs: `docs/api/date.md`

### API Reference

#### renderDatepicker(options)
Renders a date input wrapped in a `<div data-bn="datepicker">` container. **Parameters:** - `options.name` — `name` attribute for the `<input>` - `options.label` — label text; omit to render without a label - `options.value` — initial valu…
```html
<div data-bn="datepicker">
  <label for="bn-date-event_date" data-bn="label">Event date</label>
  <input type="date" id="bn-date-event_date" name="event_date"
         min="2026-01-01" max="2026-12-31" required
         data-bn="datepicker-input">
</div>
```

#### renderTimepicker(options)
Renders a time input wrapped in a `<div data-bn="timepicker">` container. **Parameters:** - `options.name` — `name` attribute - `options.label` — label text - `options.value` — initial value in `HH:MM` format; default `''` - `options.min`…

#### renderDateRange(options)
Renders a start/end date pair inside a `<fieldset data-bn="daterange">`. **Parameters:** - `options.nameStart` — `name` attribute for the start input; default `'start_date'` - `options.nameEnd` — `name` attribute for the end input; default…

#### generateCalendarMonth(year, month)
Generates a calendar grid for a given month, suitable for building custom calendar components. **Parameters:** - `year` — four-digit year number - `month` — zero-indexed month number (0 = January, 11 = December) **Returns:** Object: **Exam…
```js
{
  year: number,
  month: number,
  monthName: string,          // e.g. 'April'
  daysInMonth: number,
  weeks: Array<Array<       // 6-week grid
    null |                  // padding day
    { day: number, date: string } // date in 'YYYY-MM-DD' format
  >>
}
```

### `@basenative/db` (v0.3.0)

Query builder with SQLite, PostgreSQL, and Cloudflare D1 adapters

Source: `packages/db/src` · Docs: `docs/api/db.md`

### API Reference

#### select(table)
Starts a `SELECT` query builder for the given table. **Parameters:** - `table` — table name string **Returns:** `SelectBuilder` with chainable methods. **SelectBuilder methods:** - `.columns(...cols)` — columns to select; default `['*']` -…
```js
const { sql, params } = select('posts')
  .columns('posts.id', 'posts.title', 'users.name')
  .leftJoin('users', 'posts.user_id = users.id')
  .where('posts.published = ?', true)
  .orderBy('posts.created_at', 'DESC')
  .limit(10)
  .offset(20)
  .build();
```

#### insert(table)
Starts an `INSERT` query builder. **Parameters:** - `table` — table name string **Returns:** `InsertBuilder` with chainable methods. **InsertBuilder methods:** - `.values(data)` — object of column/value pairs - `.returning(...cols)` — `RET…

#### update(table)
Starts an `UPDATE` query builder. **Returns:** `UpdateBuilder` with `.set(data)`, `.where(condition, ...params)`, `.build()`. **Example:**
```js
const q = update('users')
  .set({ name: 'Bob' })
  .where('id = ?', 42)
  .build();

await db.execute(q.sql, q.params);
```

#### deleteFrom(table)
Starts a `DELETE` query builder. **Returns:** `DeleteBuilder` with `.where(condition, ...params)`, `.build()`.

#### raw(sql, params)
Passes a raw SQL string directly with bound parameters. **Parameters:** - `sql` — raw SQL string - `params` — array of parameter values; default `[]` **Returns:** `{ sql, params }`

#### createSqliteAdapter(options)
Creates a SQLite adapter. Uses Node 22.5+ built-in `node:sqlite`; falls back to `better-sqlite3`. **Parameters:** - `options.filename` — database file path; default `':memory:'` - `options.readonly` — open in read-only mode; default `false…

#### createPostgresAdapter(options)
Creates a PostgreSQL adapter. Requires a `pg` client instance. **Parameters:** - `options.client` — a `pg.Client` or `pg.Pool` instance **Returns:** Adapter with the same interface as `createSqliteAdapter`.

#### createD1Adapter(options)
Creates a Cloudflare D1 adapter for use in Workers. **Parameters:** - `options.db` — the D1 database binding from the Workers environment **Returns:** Adapter with the same interface as `createSqliteAdapter`.

#### migrate(adapter, migrations)
Runs pending migrations in order. **Parameters:** - `adapter` — database adapter - `migrations` — array of `{ version: number, up: string }` objects

#### rollback(adapter, migrations, steps)
Rolls back the most recent migrations. **Parameters:** - `adapter` — database adapter - `migrations` — array of `{ version: number, down: string }` objects - `steps` — number of migrations to roll back; default `1`

#### validateAdapter(adapter)
Throws if the adapter does not implement the required interface (`query`, `queryOne`, `execute`, `transaction`).

### `@basenative/doppler` (v0.2.0)

Thin developer-experience layer around Doppler — local-dev wrapper, CI helper, project bootstrap, environment validation. BaseNative ships plumbing, never values.

Source: `packages/doppler/src`

#### `dopplerRun(args, opts = {})` (function)
Wraps `doppler run -- <args...>` so callers don't have to remember the incantation. Returns a Promise that resolves with the child's exit code.
packages/doppler/src/index.js · `import { dopplerRun } from '@basenative/doppler';`

#### `requireSecrets(names, opts = {})` (async function)
Validates that the named secrets exist and are non-empty in the current Doppler context. Throws a `MissingSecretsError` listing the missing names.
packages/doppler/src/index.js · `import { requireSecrets } from '@basenative/doppler';`

#### `injectIntoWrangler({ env = process.env, names, secretNames })` (function)
Move Doppler-resolved values out of an arbitrary env object and into the shape Wrangler expects. Returns `{ vars, secrets }`: - `vars` — non-secret name/value pairs, suitable for `[vars]` in toml. - `secrets` — secret names paired with the…
packages/doppler/src/index.js · `import { injectIntoWrangler } from '@basenative/doppler';`

#### `MissingSecretsError` (class)
packages/doppler/src/index.js

#### `loadRequired(filePath)` (function)
Parse and validate a `doppler-required.json` file from disk.
packages/doppler/src/required.js · `import { loadRequired } from '@basenative/doppler';`

#### `validateRequired(input)` (function)
packages/doppler/src/required.js · `import { validateRequired } from '@basenative/doppler';`

#### `findMissing(schema, env)` (function)
Returns the names of secrets in the schema that are missing from `env`. Optional secrets (required: false) are ignored.
packages/doppler/src/required.js · `import { findMissing } from '@basenative/doppler';`

#### `ValidationError` (class)
packages/doppler/src/required.js

### `@basenative/eslint-config` (v0.2.0)

Shared ESLint flat-config for DuganLabs projects — security, hygiene, zero-noise defaults

Source: `packages/eslint-config/src`

#### `default` (default export)
packages/eslint-config/src/index.js

#### `default` (default export)
packages/eslint-config/src/browser.js

#### `default` (default export)
packages/eslint-config/src/node.js

#### `default` (default export)
packages/eslint-config/src/worker.js

#### `default` (default export)
packages/eslint-config/src/react.js

### `@basenative/favicon` (v1.0.0)

`@basenative/favicon` — SVG-first favicon generator + design primitives.

Source: `packages/favicon/src`

#### `htmlTags(opts = {})` (function)
Build the recommended `<link>` / `<meta>` tags for a favicon. The output is an array of strings — one tag per line — so consumers can join with `\n` and inject into a server-rendered `<head>`, or call `.join('')` in a Worker template.
packages/favicon/src/index.js · `import { htmlTags } from '@basenative/favicon';`

#### `defineFavicon(input)` (function)
Turn a favicon spec (or a preset name) into a ready-to-ship bundle.
packages/favicon/src/index.js · `import { defineFavicon } from '@basenative/favicon';`

#### `default` (default export)
packages/favicon/src/index.js

#### `renderFaviconSvg(spec)` (function)
Render a complete favicon SVG document.
packages/favicon/src/render.js · `import { renderFaviconSvg } from '@basenative/favicon';`

#### `renderMaskableSvg(spec)` (function)
Render a maskable variant — adds the 80% safe zone Android requires by scaling the glyph down (the body fills the full canvas as the maskable "icon background"). Output SVG stays 1024×1024.
packages/favicon/src/render.js · `import { renderMaskableSvg } from '@basenative/favicon';`

#### `renderAppleSvg(spec)` (function)
Render an apple-touch-icon SVG — Apple wants a non-transparent square, no rounded corners (iOS rounds them itself). 180×180 is the canonical size; we still emit 1024×1024 viewBox so the consumer can rasterize.
packages/favicon/src/render.js · `import { renderAppleSvg } from '@basenative/favicon';`

#### `shape(name, palette)` (function)
Draw the background "shape" — a clipped colored body sized to the viewBox.
packages/favicon/src/render.js · `import { shape } from '@basenative/favicon';`

#### `monogram(spec, palette)` (function)
Monogram glyph. Draws 1-3 letters in a tight, geometric face.
packages/favicon/src/glyphs.js · `import { monogram } from '@basenative/favicon';`

#### `symbol(spec, palette)` (function)
Symbol glyph entry point.
packages/favicon/src/glyphs.js · `import { symbol } from '@basenative/favicon';`

#### `sigil(spec, palette)` (function)
Sigil glyph entry point.
packages/favicon/src/glyphs.js · `import { sigil } from '@basenative/favicon';`

#### `wordmark(spec, palette)` (function)
Wordmark glyph. Short word in a single line. Sized to fit 1024 wide.
packages/favicon/src/glyphs.js · `import { wordmark } from '@basenative/favicon';`

#### `symbols` (const)
Hand-tuned vector symbols. Each path is drawn inside a 1024×1024 viewBox with the shape's body sitting in roughly the central 640×640 — leaves enough margin for `shape` primitives like `rounded` / `circle` to crop comfortably at 16×16.
packages/favicon/src/glyphs.js · `import { symbols } from '@basenative/favicon';`

#### `sigils` (const)
Hand-tuned abstract sigils. Each one is a recognizable signal mark.
packages/favicon/src/glyphs.js · `import { sigils } from '@basenative/favicon';`

#### `renderGlyph(kind, spec, palette)` (function)
Dispatch a glyph by `kind`.
packages/favicon/src/glyphs.js · `import { renderGlyph } from '@basenative/favicon';`

#### `resolvePalette(input)` (function)
Resolve a palette input to a fully-populated `{ bg, fg, accent }`.
packages/favicon/src/palette.js · `import { resolvePalette } from '@basenative/favicon';`

#### `hexToRgb(hex)` (function)
Parse `#rgb` / `#rrggbb` to `[r, g, b]` (0-255). Returns null on bad input.
packages/favicon/src/palette.js · `import { hexToRgb } from '@basenative/favicon';`

#### `rgbToHex([r, g, b])` (function)
Encode `[r, g, b]` back to lowercase `#rrggbb`.
packages/favicon/src/palette.js · `import { rgbToHex } from '@basenative/favicon';`

#### `luminance(hex)` (function)
Relative luminance per WCAG. 0 = black, 1 = white.
packages/favicon/src/palette.js · `import { luminance } from '@basenative/favicon';`

#### `mix(a, b, t)` (function)
Mix two hex colors at `t` (0 → a, 1 → b).
packages/favicon/src/palette.js · `import { mix } from '@basenative/favicon';`

#### `housePalette` (const)
The default DuganLabs palette.
packages/favicon/src/palette.js · `import { housePalette } from '@basenative/favicon';`

#### `buildManifest(opts)` (function)
Build a Web App Manifest object.
packages/favicon/src/manifest.js · `import { buildManifest } from '@basenative/favicon';`

#### `manifestJson(opts)` (function)
Stringify a manifest into pretty-printed JSON, with a trailing newline.
packages/favicon/src/manifest.js · `import { manifestJson } from '@basenative/favicon';`

#### `presets` (const)
packages/favicon/src/presets/dl.js · `import { presets } from '@basenative/favicon';`

#### `presetList` (const)
Ordered preset list — handy for help output.
packages/favicon/src/presets/dl.js · `import { presetList } from '@basenative/favicon';`

#### `tabs` (const)
packages/favicon/src/presets/dl.js · `import { tabs } from '@basenative/favicon';`

#### `basenative` (const)
packages/favicon/src/presets/dl.js · `import { basenative } from '@basenative/favicon';`

#### `duganlabs` (const)
packages/favicon/src/presets/dl.js · `import { duganlabs } from '@basenative/favicon';`

#### `pendingbusiness` (const)
packages/favicon/src/presets/dl.js · `import { pendingbusiness } from '@basenative/favicon';`

#### `greenput` (const)
packages/favicon/src/presets/dl.js · `import { greenput } from '@basenative/favicon';`

#### `warrendugan` (const)
packages/favicon/src/presets/dl.js · `import { warrendugan } from '@basenative/favicon';`

#### `ralphStation` (const)
packages/favicon/src/presets/dl.js · `import { ralphStation } from '@basenative/favicon';`

#### `warrenSys` (const)
packages/favicon/src/presets/dl.js · `import { warrenSys } from '@basenative/favicon';`

#### `toPng(svg, size = 512)` (async function)
Rasterize an SVG string to PNG bytes at the requested square size.
packages/favicon/src/png.js · `import { toPng } from '@basenative/favicon';`

#### `toIconSet({ favicon, maskable })` (async function)
Convenience: rasterize the canonical icon set in one call. Returns a map of filename → PNG bytes ready to write under `public/`.
packages/favicon/src/png.js · `import { toIconSet } from '@basenative/favicon';`

### `@basenative/fetch` (v0.3.0)

Signal-based resource fetching with caching and SSR preload support

Source: `packages/fetch/src` · Docs: `docs/api/fetch.md`

### API Reference

#### createResource(fetcher, options)
Creates a signal-based async resource that fetches and exposes reactive state. **Parameters:** - `fetcher` — async function `(params, { signal }) => data`; receives an `AbortSignal` for cancellation - `options.initialData` — value for the…
```js
const post = createResource(
  ({ id }) => fetchJson(`/api/posts/${id}`),
  { immediate: false }
);

await post.fetch({ id: 42 });
console.log(post.data()); // post object

// Optimistic update
post.mutate(prev => ({ ...prev, title: 'Updated' }));
```

#### createMutation(mutationFn, options)
Creates a signal-based mutation for write operations. **Parameters:** - `mutationFn` — async function `(params) => result` - `options.onSuccess(result, params)` — called after a successful mutation - `options.onError(error, params)` — call…
```js
const createUser = createMutation(
  (data) => fetchJson('/api/users', { method: 'POST', body: data }),
  {
    onSuccess: (user) => {
      console.log('Created user:', user.id);
      userList.refetch();
    },
  }
);

…
```

#### createCache(options)
Creates a TTL-based in-memory request cache for deduplication. **Parameters:** - `options.maxAge` — entry TTL in ms; default `300000` (5 minutes) - `options.maxSize` — maximum number of entries; oldest is evicted when full; default `100` *…
```js
const cache = createCache({ maxAge: 60_000 });

async function getUser(id) {
  const key = `user:${id}`;
  if (cache.has(key)) return cache.get(key);
  const user = await fetchJson(`/api/users/${id}`);
  cache.set(key, user);
  return user;
}
```

#### fetchJson(url, options)
Thin `fetch` wrapper that serializes request bodies as JSON and parses responses as JSON. **Parameters:** - `url` — request URL string - `options` — standard `fetch` options; `options.body` is passed through `JSON.stringify` - `options.hea…
```js
// GET
const users = await fetchJson('/api/users');

// POST
const created = await fetchJson('/api/posts', {
  method: 'POST',
  body: { title: 'Hello', content: 'World' },
});
```

### `@basenative/flags` (v0.3.0)

Feature flags with percentage rollouts and @feature template directive

Source: `packages/flags/src` · Docs: `docs/api/flags.md`

### API Reference

#### createFlagManager(provider, options)
Creates a feature flag manager backed by the given provider. **Parameters:** - `provider` — flag provider with `getFlag(name)` and `getAllFlags()` methods - `options.defaultValue` — value returned when a flag is not found; default `false`…

##### flagManager.isEnabled(flagName, context)
Checks whether a flag is enabled for the given context. **Parameters:** - `flagName` — flag name string - `context` — object with optional `userId`, `sessionId`, `role` fields **Returns:** `Promise<boolean>` **Evaluation order:** 1. Simple…
```js
// 20% rollout — same user always gets the same result
await flags.isEnabled('beta_ui', { userId: 'usr_123' });

// Role-based
await flags.isEnabled('admin_tools', { userId: 'usr_456', role: 'admin' });
```

##### flagManager.getAll(context)
Returns a map of all flag names to their evaluated boolean values for the given context. **Parameters:** - `context` — same shape as `isEnabled` context **Returns:** `Promise<Record<string, boolean>>` Useful for bootstrapping client-side f…

##### flagManager.setFlag(flagName, config)
Sets or updates a flag configuration (if the provider supports mutation). **Parameters:** - `flagName` — flag name string - `config` — flag configuration object

#### flagMiddleware(flagManager)
Middleware that attaches the flag manager to `ctx.state`. **Parameters:** - `flagManager` — flag manager from `createFlagManager` **Returns:** Middleware function. After this middleware runs: - `ctx.state.flags` — the flag manager instance…
```js
import { createPipeline } from '@basenative/middleware';
import { flagMiddleware } from '@basenative/flags';

const pipeline = createPipeline()
  .use(sessionMiddleware(sessions))
  .use(flagMiddleware(flags));

// In a route handler:
if (await ctx.state.isEnabled('new_checkout')) {
  // render new checkout
…
```

#### createMemoryProvider(initialFlags)
In-memory flag provider. Flag values can be updated at runtime via `setFlag`. **Parameters:** - `initialFlags` — object mapping flag names to flag config objects **Returns:** Provider object with `getFlag`, `getAllFlags`, `setFlag`, `delet…
```js
{
  enabled: boolean,        // simple on/off
  percentage: number,      // 0–100 rollout percentage
  rules: [                 // targeting rules (evaluated in order)
    {
      userIds: ['usr_1'],  // specific user IDs
      roles: ['admin'],    // specific roles
      condition: (ctx) => boolean,  // custom function
      value: true,         // value to return when this rule matches
    }
…
```

#### createRemoteProvider(options)
Fetches flags from an HTTP endpoint. Supports polling for updates. **Parameters:** - `options.url` — endpoint URL that returns `Record<string, FlagConfig>` as JSON - `options.headers` — additional request headers (e.g. for auth tokens) - `…

#### Undocumented exports (source-only — not in docs/api/flags.md)

#### `createKVProvider(options)` (function)
Cloudflare KV-backed feature flag provider with edge caching.
packages/flags/src/providers/kv.js · `import { createKVProvider } from '@basenative/flags';`

### `@basenative/forms` (v0.4.0)

Signal-based form state with dirty/touched tracking and schema validation adapters

Source: `packages/forms/src` · Docs: `docs/api/forms.md`

### `createField(initial, options?)`
Creates a reactive form field.
```js
import { createField, required, email } from '@basenative/forms';

const emailField = createField('', {
  validators: [required(), email()],
  transform: (v) => v.trim().toLowerCase(),
});
```

#### Field Properties
| Property | Type | Description | |----------|------|-------------| | `value` | `Signal` | Current field value | | `touched` | `Signal<boolean>` | Whether field has been interacted with | | `dirty` | `Signal<boolean>` | Whether value has c…

#### Field Methods
| Method | Description | |--------|-------------| | `setValue(value)` | Update field value (accepts updater fn) | | `touch()` | Mark as touched | | `reset(value?)` | Reset to initial (or provided) value | | `setServerErrors(errors)` | Set…

### `createForm(fields, options?)`
Groups fields into a form with combined state management.
```js
import { createForm } from '@basenative/forms';

const form = createForm({
  name: nameField,
  email: emailField,
}, {
  async onSubmit(values) {
    return fetch('/api', { method: 'POST', body: JSON.stringify(values) });
  },
});
```

#### Form Properties
| Property | Type | Description | |----------|------|-------------| | `fields` | `Record<string, Field>` | Named fields | | `values` | `Signal<Record>` | All field values | | `errors` | `Signal<Record>` | All field errors by name | | `vali…

#### Form Methods
| Method | Description | |--------|-------------| | `submit()` | Touch all fields and run onSubmit if valid | | `reset()` | Reset all fields | | `touchAll()` | Mark all fields as touched | | `getValues()` | Get current values snapshot | |…

### Built-in Validators
| Validator | Description | |-----------|-------------| | `required(msg?)` | Value must not be empty | | `minLength(n, msg?)` | String must be at least n characters | | `maxLength(n, msg?)` | String must be at most n characters | | `patter…

### `zodAdapter(schema)`
Adapter for Zod schemas.
```js
import { z } from 'zod';
import { zodAdapter } from '@basenative/forms';

const schema = zodAdapter(z.object({
  name: z.string().min(1),
  email: z.string().email(),
}));

const form = createForm(fields, { schema });
```

#### Undocumented exports (source-only — not in docs/api/forms.md)

#### `createWizard(steps, options = {})` (function)
Creates a multi-step form wizard that coordinates pre-built form instances.
packages/forms/src/wizard.js · `import { createWizard } from '@basenative/forms';`

### `@basenative/i18n` (v0.3.0)

ICU message formatting, locale detection, and @t template directive

Source: `packages/i18n/src` · Docs: `docs/api/i18n.md`

### API Reference

#### createI18n(options)
Creates an i18n instance. **Parameters:** - `options.defaultLocale` — fallback locale when a key is missing in the current locale; default `'en'` - `options.messages` — object mapping locale codes to message dictionaries **Returns:** i18n…

##### i18n.t(key, params)
Translates a key with optional parameter interpolation. **Parameters:** - `key` — message key string - `params` — object of interpolation values **Returns:** Translated string. Falls back to `defaultLocale` if the key is missing. Returns t…
```js
i18n.t('greeting', { name: 'Bob' });
i18n.t('items', { count: 0 }); // uses 'zero' case if defined, else 'other'
```

##### i18n.n(value, formatOptions)
Formats a number according to the current locale using `Intl.NumberFormat`. **Parameters:** - `value` — number to format - `formatOptions` — `Intl.NumberFormat` options **Returns:** Formatted number string. **Example:**
```js
i18n.setLocale('de');
i18n.n(1234567.89, { style: 'currency', currency: 'EUR' }); // '1.234.567,89 €'
```

##### i18n.d(value, formatOptions)
Formats a date according to the current locale using `Intl.DateTimeFormat`. **Parameters:** - `value` — `Date` object or value parseable by `new Date()` - `formatOptions` — `Intl.DateTimeFormat` options **Returns:** Formatted date string.

##### i18n.setLocale(locale)
Sets the active locale. Notifies all `onLocaleChange` listeners.

##### i18n.getLocale()
Returns the current locale string.

##### i18n.onLocaleChange(fn)
Registers a listener called whenever the locale changes. **Parameters:** - `fn` — function `(locale: string) => void` **Returns:** Unsubscribe function.

##### i18n.addMessages(locale, messages)
Merges additional messages into a locale. Useful for lazy loading. **Parameters:** - `locale` — locale code string - `messages` — object of key/value message pairs

#### i18nMiddleware(i18n, options)
Middleware that detects the locale from the request and attaches the i18n instance to context. Detection order (first match wins): 1. URL query parameter (default: `locale`) 2. Cookie (default: `locale`) 3. `Accept-Language` header (respec…

#### createLoader(options)
Creates a lazy message loader for code-split locale bundles. **Parameters:** - `options.load(locale)` — async function that returns a message dictionary for the given locale **Returns:** Loader object.

#### loadMessages(locale, path)
Loads messages from a JSON file. **Parameters:** - `locale` — locale code string - `path` — path to the JSON message file **Returns:** `Promise<object>` — message dictionary.

### `@basenative/integrations` (v0.1.0)

Headless third-party integration wrappers for BaseNative apps

Source: `packages/integrations/src`

#### `loadPlaidScript()` (function)
Load the Plaid Link drop-in script if not already present. Returns a promise that resolves when the script is ready.
packages/integrations/src/plaid.js · `import { loadPlaidScript } from '@basenative/integrations';`

#### `createPlaidLink(config)` (async function)
Create a signal-based Plaid Link handler. Returns signals for linkToken, linkStatus, and accounts.
packages/integrations/src/plaid.js · `import { createPlaidLink } from '@basenative/integrations';`

#### `openPlaidLink(options)` (async function)
Open Plaid Link with the given link token (legacy API).
packages/integrations/src/plaid.js · `import { openPlaidLink } from '@basenative/integrations';`

#### `createPlaidClient(config)` (function)
Create a Plaid API client factory with server methods. Works in Cloudflare Workers and Node.js environments.
packages/integrations/src/plaid.js · `import { createPlaidClient } from '@basenative/integrations';`

#### `createLinkToken(options, credentials)` (async function)
Create a Plaid Link token for client-side initialization.
packages/integrations/src/plaid.js · `import { createLinkToken } from '@basenative/integrations';`

#### `exchangePublicToken(publicToken, credentials)` (async function)
Exchange a public token (from Link onSuccess) for an access token.
packages/integrations/src/plaid.js · `import { exchangePublicToken } from '@basenative/integrations';`

#### `getAccounts(accessToken, credentials)` (async function)
Fetch linked accounts (without balance details).
packages/integrations/src/plaid.js · `import { getAccounts } from '@basenative/integrations';`

#### `getBalances(accessToken, credentials)` (async function)
Fetch account balances for a linked item.
packages/integrations/src/plaid.js · `import { getBalances } from '@basenative/integrations';`

#### `getBalance(accessToken, credentials)` (async function)
Alias for getBalances() for consistency.
packages/integrations/src/plaid.js · `import { getBalance } from '@basenative/integrations';`

#### `createTransfer(params, credentials)` (async function)
Initiate a transfer via Plaid Transfer API. Supports FedNow instant transfers (network: "rtp") with graceful fallback to ACH.
packages/integrations/src/plaid.js · `import { createTransfer } from '@basenative/integrations';`

#### `getTransferStatus(transferId, credentials)` (async function)
Check the status of a transfer.
packages/integrations/src/plaid.js · `import { getTransferStatus } from '@basenative/integrations';`

#### `findBestYieldAccount(accounts)` (function)
Find the best yield account from a list of linked accounts with balances and APYs.
packages/integrations/src/yield.js · `import { findBestYieldAccount } from '@basenative/integrations';`

#### `optimizeFloatAllocation(accounts, totalFloat)` (function)
Determine optimal float allocation across multiple accounts.
packages/integrations/src/yield.js · `import { optimizeFloatAllocation } from '@basenative/integrations';`

#### `calculateOptimalTransferTiming(liabilities, accounts)` (function)
Calculate optimal timing for float transfers based on liability due dates and yield.
packages/integrations/src/yield.js · `import { calculateOptimalTransferTiming } from '@basenative/integrations';`

#### `calculateBreakEven(amount, fromApy, toApy, transferCostCents = 0, daysFloat = 30)` (function)
Calculate the break-even point for a transfer based on yield difference.
packages/integrations/src/yield.js · `import { calculateBreakEven } from '@basenative/integrations';`

### `@basenative/keyboard` (v1.0.1)

@basenative/keyboard — accessible mobile-first virtual keyboard primitive.

Source: `packages/keyboard/src`

- `./css` → `./src/styles.css` (non-JS resource export)
- `./styles.css` → `./src/styles.css` (non-JS resource export)

#### `Keyboard(options = {})` (function)
One-shot convenience. Returns `{ html, hydrate }` so callers can embed the markup in SSR output and wire up reactivity later.
packages/keyboard/src/keyboard.js · `import { Keyboard } from '@basenative/keyboard';`

#### `renderKeyboard(options = {})` (function)
Render the keyboard as an HTML string. Pre-paint friendly — works in SSR, hydrates cleanly client-side.
packages/keyboard/src/keyboard.js · `import { renderKeyboard } from '@basenative/keyboard';`

#### `hydrateKeyboard(rootEl, options = {})` (function)
Hydrate a rendered keyboard. `rootEl` is the element produced by `renderKeyboard` (or any equivalent DOM that follows the `data-bn="kb-key"` contract).
packages/keyboard/src/keyboard.js · `import { hydrateKeyboard } from '@basenative/keyboard';`

#### `keyState(stateOrGetter, letter)` (function)
Lift a single letter's status out of a state-map signal/getter. Useful when you want to drive a *different* UI element off the same key-status source (e.g. a tile in a Wordle-style grid).
packages/keyboard/src/keyboard.js · `import { keyState } from '@basenative/keyboard';`

#### `LAYOUTS` (const)
Built-in layouts. All values are pre-normalized.
packages/keyboard/src/layouts.js · `import { LAYOUTS } from '@basenative/keyboard';`

#### `defineLayout(rows, meta = {})` (function)
Define a custom layout. Validates row shape and returns a layout object usable in `Keyboard({ layout })`.
packages/keyboard/src/layouts.js · `import { defineLayout } from '@basenative/keyboard';`

#### `validateLayout(layout)` (function)
Shape-validate a layout object. Returns true or throws with a clear message — handy in tests and dev builds.
packages/keyboard/src/layouts.js · `import { validateLayout } from '@basenative/keyboard';`

#### `normalizeKey(raw)` (function)
Normalize any user-supplied key shape into a canonical Key object. Strings become char keys; partial objects get sensible defaults.
packages/keyboard/src/layouts.js · `import { normalizeKey } from '@basenative/keyboard';`

#### `applyAriaAttributes(rootEl, options = {})` (function)
Apply accessible attributes to the root keyboard element. Idempotent — safe to call after re-render or on hydrate.
packages/keyboard/src/a11y.js · `import { applyAriaAttributes } from '@basenative/keyboard';`

#### `preventFocusSteal(rootEl)` (function)
Mobile-Safari focus fix. Without this, tapping a key briefly blurs the active text input, which makes iOS think the user dismissed the field — and on the next focus it re-raises the native keyboard. We swallow the mousedown so focus never…
packages/keyboard/src/a11y.js · `import { preventFocusSteal } from '@basenative/keyboard';`

#### `bindHardwareKeys(target, handlers, options = {})` (function)
Bind a hardware keyboard listener that mirrors physical keys onto the same dispatch pipeline as the on-screen keyboard.
packages/keyboard/src/a11y.js · `import { bindHardwareKeys } from '@basenative/keyboard';`

#### `haptic(ms = 8)` (function)
Optional haptic feedback. No-ops where unsupported.
packages/keyboard/src/a11y.js · `import { haptic } from '@basenative/keyboard';`

### `@basenative/logger` (v0.3.0)

Structured logging with multiple transports and child logger support

Source: `packages/logger/src` · Docs: `docs/api/logger.md`

### API Reference

#### createLogger(options)
Creates a logger instance. **Parameters:** - `options.level` — minimum log level; one of `'trace'`, `'debug'`, `'info'`, `'warn'`, `'error'`, `'fatal'`; default `'info'` - `options.name` — logger name; included in every log entry - `option…

#### logger.child(childContext)
Creates a child logger that inherits all options and prepends `childContext` to every log entry. **Parameters:** - `childContext` — object of key/value pairs to add to every entry **Returns:** New logger instance. **Example:**
```js
const reqLogger = logger.child({ requestId: 'abc123', userId: 42 });
reqLogger.info('Processing request'); // { requestId: 'abc123', userId: 42, msg: 'Processing request', ... }
```

#### consoleTransport()
Default transport. Writes pretty-printed output in development and JSON lines in production. **Returns:** Transport object with `write(entry, pretty)` method.

#### streamTransport(stream)
Writes JSON lines to any Node.js writable stream. **Parameters:** - `stream` — Node.js `Writable` stream (e.g. `fs.createWriteStream`) **Returns:** Transport object. **Example:**
```js
import { createWriteStream } from 'node:fs';

const logger = createLogger({
  transport: streamTransport(createWriteStream('./app.log', { flags: 'a' })),
});
```

#### multiTransport(transports)
Fan-out transport that writes to multiple transports simultaneously. **Parameters:** - `transports` — array of transport instances **Returns:** Transport object. **Example:**
```js
import { createLogger, consoleTransport, streamTransport, multiTransport } from '@basenative/logger';
import { createWriteStream } from 'node:fs';

const logger = createLogger({
  transport: multiTransport([
    consoleTransport(),
    streamTransport(createWriteStream('./errors.log')),
  ]),
});
```

#### requestLogger(logger, options)
Creates middleware that attaches a child logger to each request context. **Parameters:** - `logger` — logger instance - `options.idHeader` — header name for request ID; default `'x-request-id'` **Returns:** Middleware function `(ctx, next)…
```js
import { createPipeline } from '@basenative/middleware';
import { createLogger, requestLogger } from '@basenative/logger';

const logger = createLogger({ name: 'api' });
const pipeline = createPipeline();
pipeline.use(requestLogger(logger));

// In later middleware:
ctx.state.logger.info('User fetched', { userId: 42 });
```

### Log Entry Format
In production (JSON mode): In development (pretty mode): Level numbers follow the Pino convention (10–60), making logs compatible with Pino transports and log aggregation tools.
```json
{"level":30,"time":1712345678901,"name":"app","msg":"Server started","port":3000}
```

#### Undocumented exports (source-only — not in docs/api/logger.md)

#### `LEVELS` (const)
Structured logging with Pino-compatible JSON output.
packages/logger/src/index.js · `import { LEVELS } from '@basenative/logger';`

#### `LEVEL_NAMES` (const)
packages/logger/src/index.js · `import { LEVEL_NAMES } from '@basenative/logger';`

### `@basenative/markdown` (v0.1.0)

Zero-dependency ES module markdown parser and SSR-safe HTML renderer

Source: `packages/markdown/src`

#### `parse(markdown)` (function)
Parse markdown to an HTML string. Convenience wrapper that runs the parser and renderer back-to-back.
packages/markdown/src/index.js · `import { parse } from '@basenative/markdown';`

#### `parseFrontmatter(input)` (function)
Extract YAML-style frontmatter from a markdown document.
packages/markdown/src/index.js · `import { parseFrontmatter } from '@basenative/markdown';`

#### `parseAST(markdown)` (function)
packages/markdown/src/parser.js · `import { parseAST } from '@basenative/markdown';`

#### `render(ast)` (function)
HTML renderer for the markdown AST. SSR-safe: produces a plain HTML string with no DOM access, escapes text content, and rejects dangerous URL schemes.
packages/markdown/src/renderer.js · `import { render } from '@basenative/markdown';`

### `@basenative/marketplace` (v0.2.0)

Component registry for BaseNative — publishing and discovery infrastructure

Source: `packages/marketplace/src` · Docs: `docs/api/marketplace.md`

### API Reference

#### createRegistry(options)
Creates a marketplace registry client. **Options:** - `url` — registry base URL (default: `'https://registry.basenative.dev'`) - `token` — optional auth token for private packages **Returns:** `Registry` with: - `search(query, options?)` —…

#### createInstaller(options)
Installs components from the registry into your project. **Options:** - `outputDir` — directory to install component files into - `registry` — optional `Registry` instance (creates one if omitted) **Returns:** `Installer` with: - `install(…

#### createThemeManager(options)
Manages theme discovery and application from the registry. **Options:** - `registry` — `Registry` instance **Returns:** `ThemeManager` with: - `list()` — list available themes - `apply(themeId)` — download theme CSS and return the styleshe…

### Registry API
The registry backend accepts: - `GET /search?q=<query>&page=<n>&limit=<n>` — search components - `GET /packages/<name>` — get package metadata - `GET /packages/<name>/<version>` — get specific version - `POST /publish` — publish (requires…

#### Undocumented exports (source-only — not in docs/api/marketplace.md)

#### `renderPackageCard(pkg)` (function)
Renders a marketplace package card as an HTML string. Works on both server (SSR) and client via @basenative/components convention.
packages/marketplace/src/card.js · `import { renderPackageCard } from '@basenative/marketplace';`

#### `packageCardStyles()` (function)
CSS for package cards. Include once on the page.
packages/marketplace/src/card.js · `import { packageCardStyles } from '@basenative/marketplace';`

### `@basenative/mcp` (v0.1.0)

MCP server exposing BaseNative validation, SSR preview, and directive reference to any agent

Source: `packages/mcp/src`

#### `TOOLS` (const)
packages/mcp/src/tools.js · `import { TOOLS } from '@basenative/mcp';`

#### `TOOL_MAP` (const)
packages/mcp/src/tools.js · `import { TOOL_MAP } from '@basenative/mcp';`

#### `handleMessage(msg)` (function)
Handle one JSON-RPC message and return the response, or null for a notification.
packages/mcp/src/server.js · `import { handleMessage } from '@basenative/mcp';`

#### `serve({ input = process.stdin, output = process.stdout } = {})` (function)
Run the stdio loop. Messages are newline-delimited JSON, which is what the Claude Code / MCP stdio transport emits.
packages/mcp/src/server.js · `import { serve } from '@basenative/mcp';`

#### `PROTOCOL_VERSION` (const)
packages/mcp/src/server.js · `import { PROTOCOL_VERSION } from '@basenative/mcp';`

#### `SERVER_INFO` (const)
packages/mcp/src/server.js · `import { SERVER_INFO } from '@basenative/mcp';`

#### `DIRECTIVES` (const)
The BaseNative directive reference, in one place.
packages/mcp/src/directives.js · `import { DIRECTIVES } from '@basenative/mcp';`

#### `PRIMITIVES` (const)
Reactivity primitives, exported from @basenative/runtime.
packages/mcp/src/directives.js · `import { PRIMITIVES } from '@basenative/mcp';`

#### `FORBIDDEN` (const)
Syntax from other frameworks that BaseNative does NOT accept.
packages/mcp/src/directives.js · `import { FORBIDDEN } from '@basenative/mcp';`

### `@basenative/middleware` (v0.3.0)

Request pipeline middleware — CORS, rate limiting, CSRF, and adapters

Source: `packages/middleware/src` · Docs: `docs/api/middleware.md`

### API Reference

#### createPipeline()
Creates a middleware pipeline. **Returns:** Pipeline object. **Pipeline methods:** - `.use(middleware)` — registers a middleware function; returns the pipeline for chaining - `.run(ctx)` — executes the pipeline with the given context; retu…
```js
const pipeline = createPipeline();
pipeline.use(async (ctx, next) => {
  console.log(ctx.request.method, ctx.request.url);
  await next();
});
await pipeline.run({ request, response, state: {} });
```

#### compose(...middlewares)
Combines multiple middleware functions into a single middleware function. **Parameters:** - `...middlewares` — middleware functions or arrays of middleware functions **Returns:** `async (ctx, next) => void` **Example:**
```js
import { compose } from '@basenative/middleware';

const combined = compose(logger(), cors(), rateLimit());
pipeline.use(combined);
```

#### cors(options)
CORS middleware. **Parameters:** - `options.origin` — `'*'`, a string, an array of strings, or a function `(origin) => boolean`; default `'*'` - `options.methods` — allowed HTTP methods; default all standard methods - `options.allowedHeade…

#### rateLimit(options)
In-memory sliding-window rate limiter. **Parameters:** - `options.max` — maximum requests per window; default `100` - `options.windowMs` — window duration in ms; default `60000` - `options.keyFn` — function `(ctx) => string` for generating…

#### csrf(options)
CSRF token middleware. Issues a token and validates it on mutating requests. **Parameters:** - `options.cookie` — cookie name for the CSRF token; default `'csrf_token'` - `options.header` — request header to check; default `'x-csrf-token'`…

#### logger(options)
Request logging middleware. Logs method, URL, status, and duration. **Parameters:** - `options.log` — log function `(message, data) => void`; default `console.log` **Returns:** Middleware function.

#### toExpressMiddleware(pipeline)
Adapts a pipeline or middleware function for use with Express. **Parameters:** - `pipeline` — a pipeline instance or middleware function **Returns:** Express-compatible `(req, res, next) => void` function.

#### toHonoMiddleware(pipeline)
Adapts a pipeline for Hono. **Returns:** Hono-compatible middleware.

#### toFastifyPlugin(pipeline)
Adapts a pipeline as a Fastify plugin. **Returns:** Fastify plugin function.

#### toCloudflareHandler(pipeline)
Adapts a pipeline for Cloudflare Workers `fetch` handler. **Returns:** `(request: Request, env, ctx) => Promise<Response>` function.

### Context Shape
All middleware receives a `ctx` object with this structure:
```js
{
  request: {
    method: string,
    url: string,
    path: string,
    headers: object,
    body: any,
    cookies: object,
  },
  response: {
…
```

#### Undocumented exports (source-only — not in docs/api/middleware.md)

#### `createExpressContext(req, _res)` (function)
Create a BaseNative context from Express req/res.
packages/middleware/src/adapters/express.js · `import { createExpressContext } from '@basenative/middleware';`

### `@basenative/notify` (v0.3.0)

Email notifications via SMTP and SendGrid with template rendering

Source: `packages/notify/src` · Docs: `docs/api/notify.md`

### API Reference

#### renderEmail(template, data)
Renders an HTML email template with `{{ variable }}` interpolation and automatically generates a plain-text version. **Parameters:** - `template` — HTML string with `{{ key }}` placeholders - `data` — key/value pairs for interpolation **Re…

#### createEmailSender(transport)
Creates an email sender that delegates to the provided transport. **Parameters:** - `transport` — any transport object with a `send(email) => Promise<any>` method **Returns:** Object with a single `send(options)` method. **send options:**…

#### createSmtpTransport(config)
Zero-dependency SMTP transport using Node.js built-in `node:net` and `node:tls`. **Parameters:** - `config.host` — SMTP server hostname - `config.port` — SMTP server port - `config.secure` — use TLS (SSL); default `false` - `config.auth.us…
```js
const transport = createSmtpTransport({
  host: 'smtp.gmail.com',
  port: 465,
  secure: true,
  auth: { user: 'me@gmail.com', pass: 'app-password' },
});
```

#### createSendGridTransport(config)
SendGrid email transport. **Parameters:** - `config.apiKey` — SendGrid API key **Returns:** Transport object. **Example:**
```js
import { createSendGridTransport, createEmailSender } from '@basenative/notify';

const mailer = createEmailSender(
  createSendGridTransport({ apiKey: process.env.SENDGRID_API_KEY })
);
```

#### createResendTransport(config)
Resend email transport. **Parameters:** - `config.apiKey` — Resend API key **Returns:** Transport object.

#### createNotificationCenter()
Creates an in-app notification center for managing per-user notification queues. **Returns:** Notification center object with methods for creating, listing, and marking notifications as read.

### `@basenative/og-image` (v0.2.0)

`@basenative/og-image` — runtime OG / social share PNG renderer for Cloudflare Workers and Pages Functions.

Source: `packages/og-image/src`

#### `renderPng(scene, env, opts = {})` (async function)
Render a satori-compatible scene to PNG bytes.
packages/og-image/src/index.js · `import { renderPng } from '@basenative/og-image';`

#### `pngHeaders(opts = {})` (function)
Standard headers for an OG PNG response. By default the response is marked immutable (one-year cache) — flip `immutable: false` for short- lived previews.
packages/og-image/src/index.js · `import { pngHeaders } from '@basenative/og-image';`

#### `defineFonts(cfg = {})` (function)
Define a font configuration. Returned object is consumed by `loadFonts`.
packages/og-image/src/fonts.js · `import { defineFonts } from '@basenative/og-image';`

#### `box(style, children = [])` (function)
A flexbox `<div>`. Satori requires `display: flex` on every container, so this helper enforces it.
packages/og-image/src/scene.js · `import { box } from '@basenative/og-image';`

#### `text(style, content)` (function)
A text node. Satori treats text-bearing divs the same as any flex container; we keep the helper distinct for readability.
packages/og-image/src/scene.js · `import { text } from '@basenative/og-image';`

#### `tile(state, opts = {})` (function)
Render a single mini-tile (used by `tileGrid` and decorative rows).
packages/og-image/src/scene.js · `import { tile } from '@basenative/og-image';`

#### `tileGrid(rows, opts = {})` (function)
Lay out a grid of tiles.
packages/og-image/src/scene.js · `import { tileGrid } from '@basenative/og-image';`

#### `parseGrid(gridString)` (function)
Parse the emoji grid that share-strings use back into tile states. Glyph map: 🟩 green, 🟨 yellow, ⬛ absent, ⬜ empty.
packages/og-image/src/scene.js · `import { parseGrid } from '@basenative/og-image';`

#### `theme(tokens = {})` (function)
Pre-bind the scene helpers to a theme. Returns helpers that automatically thread the theme through to anything that consumes tokens (`tile`, `tileGrid`).
packages/og-image/src/scene.js · `import { theme } from '@basenative/og-image';`

#### `defaultTheme` (const)
Default theme tokens — match the t4bs palette as a sensible starting point. Override per-call via `theme()` or by passing a `theme` arg to a preset.
packages/og-image/src/scene.js · `import { defaultTheme } from '@basenative/og-image';`

#### `el(type, style, children)` (function)
Create a vh-tree element. Low-level — most callers want `box` or `text`.
packages/og-image/src/scene.js · `import { el } from '@basenative/og-image';`

#### `defaultPreset(opts)` (function)
Default brand / hero card. Title + subtitle + decorative tile row.
packages/og-image/src/presets.js · `import { defaultPreset } from '@basenative/og-image';`

#### `articlePreset(opts)` (function)
Article / blog post card. Kicker on top, headline dominant, byline below.
packages/og-image/src/presets.js · `import { articlePreset } from '@basenative/og-image';`

#### `scoreCardPreset(opts)` (function)
Game-style score card. Lifts the t4bs design: brand + verdict at top, tile-grid summary on the left and the score number on the right, brand pinned bottom-right.
packages/og-image/src/presets.js · `import { scoreCardPreset } from '@basenative/og-image';`

#### `presets` (const)
The full preset map — handy for table-driven dispatch.
packages/og-image/src/presets.js · `import { presets } from '@basenative/og-image';`

### `@basenative/persist` (v1.0.0)

@basenative/persist — TTL-aware local persistence + signal binding + server-rehydrate hook.

Source: `packages/persist/src`

#### `setStorageAdapter(a)` (function)
Override the global storage adapter (e.g. switch to IndexedDB or memory for tests). Pass `null` to reset.
packages/persist/src/index.js · `import { setStorageAdapter } from '@basenative/persist';`

#### `loadPersisted(key, opts = {})` (async function)
Read a key. Honors the TTL envelope; returns null if expired or absent.
packages/persist/src/index.js · `import { loadPersisted } from '@basenative/persist';`

#### `savePersisted(key, value, ttlSeconds)` (async function)
Write a key. `ttlSeconds` of `0`/`undefined` means no expiry.
packages/persist/src/index.js · `import { savePersisted } from '@basenative/persist';`

#### `clearPersisted(key)` (async function)
packages/persist/src/index.js · `import { clearPersisted } from '@basenative/persist';`

#### `persistedSavedAt(key)` (async function)
Read the saved-at timestamp without deserialising the full payload.
packages/persist/src/index.js · `import { persistedSavedAt } from '@basenative/persist';`

#### `persisted(key, signal, opts = {})` (function)
Bidirectional bind between a signal and a persistence key.
packages/persist/src/index.js · `import { persisted } from '@basenative/persist';`

#### `hydrateFromServer(args)` (async function)
Combine local-cache read + server fetch + reconcile.
packages/persist/src/index.js · `import { hydrateFromServer } from '@basenative/persist';`

#### `wrap(value, ttlSeconds, now = Date.now)` (function)
Wrap a value in a TTL envelope.
packages/persist/src/ttl.js · `import { wrap } from '@basenative/persist';`

#### `unwrap(envelope, now = Date.now)` (function)
Unwrap an envelope. Returns null if the value has expired or the envelope is malformed.
packages/persist/src/ttl.js · `import { unwrap } from '@basenative/persist';`

#### `fromLegacy(legacy, defaultTtlSeconds = 12 * 3600)` (function)
Migrate the t4bs-style legacy `{...state, savedAt}` shape into the current envelope, keeping the original 12h-from-savedAt expiry.
packages/persist/src/ttl.js · `import { fromLegacy } from '@basenative/persist';`

#### `defaultAdapter(opts = {})` (function)
Pick a sensible default adapter for the current environment.
packages/persist/src/storage.js · `import { defaultAdapter } from '@basenative/persist';`

#### `localStorageAdapter()` (function)
Synchronous-localStorage-backed adapter, normalised to async.
packages/persist/src/storage.js · `import { localStorageAdapter } from '@basenative/persist';`

#### `memoryAdapter()` (function)
In-memory adapter for SSR / tests.
packages/persist/src/storage.js · `import { memoryAdapter } from '@basenative/persist';`

#### `indexedDbAdapter(opts = {})` (function)
IndexedDB-backed adapter for payloads above the ~5MB localStorage cap. Falls back to memory if IndexedDB is unavailable.
packages/persist/src/storage.js · `import { indexedDbAdapter } from '@basenative/persist';`

#### `savedAt(envelope)` (function)
Read the saved-at timestamp from an envelope (legacy or current format). Returns 0 if missing.
packages/persist/src/ttl.js · `import { savedAt } from '@basenative/persist';`

#### `NO_EXPIRY` (const)
TTL envelope helpers.
packages/persist/src/ttl.js · `import { NO_EXPIRY } from '@basenative/persist';`

### `@basenative/realtime` (v0.3.0)

SSE and WebSocket connections with signal-based reactive message streams

Source: `packages/realtime/src` · Docs: `docs/api/realtime.md`

### API Reference

#### createSSEServer(options)
Creates an SSE server that manages connections and broadcasts events. **Parameters:** - `options.heartbeatInterval` — interval in ms for sending keep-alive comments; default `30000`; set to `0` to disable **Returns:** SSE server object.

##### sseServer.addClient(res, options)
Registers a new SSE client and sends the required HTTP headers. **Parameters:** - `res` — Node.js `ServerResponse` object - `options.id` — custom client ID; auto-generated if omitted - `options.metadata` — arbitrary metadata stored with th…

##### sseServer.send(clientId, event, data)
Sends an event to a specific client. **Parameters:** - `clientId` — client ID returned by `addClient` - `event` — event name string (sent as `event:` field); pass `null` for unnamed events - `data` — string or JSON-serializable value (sent…

##### sseServer.broadcast(event, data)
Sends an event to all connected clients. **Parameters:** - `event` — event name - `data` — string or JSON-serializable value **Returns:** Number of clients reached.

##### sseServer.removeClient(clientId)
Disconnects and removes a client. **Returns:** `true` if the client existed.

##### sseServer.getClients()
Returns an array of `{ id, metadata }` for all connected clients.

##### sseServer.close()
Disconnects all clients and stops the heartbeat timer.

#### sseMiddleware(sseServer)
Express-compatible middleware that upgrades requests with `Accept: text/event-stream` to SSE connections. **Parameters:** - `sseServer` — SSE server instance **Returns:** Middleware function `(req, res, next) => void`. After upgrade: - `re…
```js
app.get('/events', sseMiddleware(sse), (req, res) => {
  // req.sseClientId is set; response is held open
});
```

#### createChannelManager()
Creates a channel manager for grouping clients into named channels. **Returns:** Channel manager object.

##### channelManager.join(channelName, clientId)
Adds a client to a channel, creating it if necessary. **Returns:** `true`

##### channelManager.leave(channelName, clientId)
Removes a client from a channel. **Returns:** `true` if the client was removed.

##### channelManager.broadcast(channelName, event, data, sendFn)
Broadcasts to all members of a channel using a caller-supplied send function. **Parameters:** - `channelName` — channel name string - `event` — event name - `data` — event payload - `sendFn` — function `(clientId, event, data) => boolean`…

##### channelManager.getMembers(channelName)
Returns an array of client IDs in a channel.

##### channelManager.removeFromAll(clientId)
Removes a client from every channel. Call this when a connection closes. **Returns:** Number of channels the client was removed from.

##### channelManager.deleteChannel(channelName)
Removes a channel and all its member references.

##### channelManager.getChannels()
Returns an array of all channel name strings.

#### createWSHandler(options)
Creates a WebSocket connection handler with message routing. **Parameters:** - `options.onConnect(ws, req)` — called when a client connects - `options.onDisconnect(ws, code, reason)` — called when a client disconnects - `options.onMessage(…

### `@basenative/router` (v0.4.1)

SSR-aware path routing with named params, wildcards, and history API integration

Source: `packages/router/src` · Docs: `docs/api/router.md`

### `createRouter(routes, options?)`
Creates a client-side router with signal-based reactive state.
```js
import { createRouter } from '@basenative/router';

const router = createRouter([
  { path: '/', name: 'home' },
  { path: '/users', name: 'users' },
  { path: '/users/:id', name: 'user-detail' },
  { path: '/files/*path', name: 'files' },
], { base: '/app' });
```

#### Router Instance
| Property/Method | Type | Description | |----------------|------|-------------| | `pathname` | `Signal<string>` | Current pathname | | `query` | `Signal<Record>` | Current query params | | `currentRoute` | `Signal<ResolvedRoute>` | Curren…

#### Route Patterns
| Pattern | Example | Matches | |---------|---------|---------| | `/users` | `/users` | Static path | | `/users/:id` | `/users/42` | Named param | | `/files/*path` | `/files/a/b/c` | Wildcard |

### `resolveRoute(routes, url, options?)`
Server-side route resolution without browser APIs.
```js
const match = resolveRoute(routes, '/users/42?tab=posts');
// { name: 'user-detail', params: { id: '42' }, query: { tab: 'posts' } }
```

### `interceptLinks(root, router, options?)`
Intercepts internal link clicks for client-side navigation.
```js
const cleanup = interceptLinks(document.body, router);
```

### `@basenative/runtime` (v0.4.1)

Signal-based web runtime over native HTML — zero build step, zero production dependencies

Source: `packages/runtime/src` · Docs: `docs/api/runtime.md`

### `signal(initial)`
Creates a reactive signal.
```js
const count = signal(0);
count();        // read: 0
count.set(5);   // write
count.set(c => c + 1); // updater
count.peek();   // read without subscribing
```

### `computed(fn)`
Creates a derived signal that re-evaluates when dependencies change.
```js
const double = computed(() => count() * 2);
```

### `effect(fn)`
Runs a function reactively. Re-runs when any signal read inside changes. Returns a handle with `.dispose()`.
```js
const stop = effect(() => {
  console.log('Count is', count());
  return () => console.log('cleanup');
});
stop.dispose(); // stops the effect
```

### `hydrate(root, ctx, options?)`
Activates template directives in a DOM subtree. Returns a dispose function.
```js
const dispose = hydrate(document.getElementById('app'), {
  count,
  items: signal([]),
  increment() { count.set(c => c + 1); },
});
```

#### Options
| Option | Type | Description | |--------|------|-------------| | `onDiagnostic` | `(diagnostic) => void` | Callback for diagnostic events | | `onMismatch` | `(message, detail) => void` | Callback for hydration mismatches | | `recover` | `…

#### `@if` / `@else`
```html
<template @if="isLoggedIn()">
  <p>Welcome back!</p>
</template>
<template @else>
  <p>Please log in.</p>
</template>
```

#### `@for` / `@empty`
Loop variables: `$index`, `$first`, `$last`, `$even`, `$odd`.
```html
<template @for="item of items(); track item.id">
  <div>{{ item.name }}</div>
</template>
<template @empty>
  <p>No items.</p>
</template>
```

#### `@switch` / `@case` / `@default`
```html
<template @switch="status()">
  <template @case="'active'"><span>Active</span></template>
  <template @case="'inactive'"><span>Inactive</span></template>
  <template @default><span>Unknown</span></template>
</template>
```

#### Event Binding — `@event`
```html
<button @click="handleClick()">Click</button>
<input @input="updateValue($event.target.value)">
```

#### Attribute Binding — `:attr`
```html
<button :disabled="isLoading()">Save</button>
<div :class="isActive() ? 'active' : ''">Content</div>
```

#### Text Interpolation — `{{ }}`
```html
<p>Hello, {{ name() }}!</p>
<span>{{ items().length }} items</span>
```

### `detectBrowserFeatures()`
Returns a `BrowserFeatures` object with support flags.

### `supportsFeature(name)`
Checks a single feature: `'dialog'`, `'popover'`, `'anchorPositioning'`, `'baseSelect'`.

### `emitDiagnostic(options, diagnostic)`
Emits a structured diagnostic through the options callback.

### `reportHydrationMismatch(options, message, detail?)`
Reports a hydration mismatch event.

#### Undocumented exports (source-only — not in docs/api/runtime.md)

#### `batch(fn)` (function)
packages/runtime/src/signals.js · `import { batch } from '@basenative/runtime';`

#### `registerPlugin(plugin)` (function)
packages/runtime/src/signals.js · `import { registerPlugin } from '@basenative/runtime';`

#### `enableDevtools()` (function)
Enables devtools instrumentation. Call this before creating signals or hydrating.
packages/runtime/src/devtools.js · `import { enableDevtools } from '@basenative/runtime';`

#### `disableDevtools()` (function)
Disables devtools and cleans up.
packages/runtime/src/devtools.js · `import { disableDevtools } from '@basenative/runtime';`

#### `isDevtoolsEnabled()` (function)
Returns whether devtools are enabled.
packages/runtime/src/devtools.js · `import { isDevtoolsEnabled } from '@basenative/runtime';`

#### `trackSignal(accessor, label)` (function)
Registers a signal for devtools inspection.
packages/runtime/src/devtools.js · `import { trackSignal } from '@basenative/runtime';`

#### `trackEffect(handle, label)` (function)
Registers an effect for devtools inspection.
packages/runtime/src/devtools.js · `import { trackEffect } from '@basenative/runtime';`

#### `recordHydration(root, details = {})` (function)
Records a hydration event for the timeline.
packages/runtime/src/devtools.js · `import { recordHydration } from '@basenative/runtime';`

#### `createErrorBoundary(options = {})` (function)
Creates an error boundary that wraps template rendering.
packages/runtime/src/error-boundary.js · `import { createErrorBoundary } from '@basenative/runtime';`

#### `renderWithBoundary(renderFn, options = {})` (function)
Server-side error boundary for render(). Wraps template rendering and returns fallback HTML on error.
packages/runtime/src/error-boundary.js · `import { renderWithBoundary } from '@basenative/runtime';`

#### `definePlugin(config)` (function)
Define a plugin with a name and setup function.
packages/runtime/src/plugins.js · `import { definePlugin } from '@basenative/runtime';`

#### `createPluginRegistry()` (function)
Create a plugin registry that manages plugin registration, lifecycle hooks, and custom directives.
packages/runtime/src/plugins.js · `import { createPluginRegistry } from '@basenative/runtime';`

#### `createLazyHydrator(options = {})` (function)
createLazyHydrator(options) — Creates a lazy hydration controller.
packages/runtime/src/lazy.js · `import { createLazyHydrator } from '@basenative/runtime';`

#### `lazyHydrate(element, hydrateFn, options = {})` (function)
Convenience wrapper — hydrate a single element when it becomes visible.
packages/runtime/src/lazy.js · `import { lazyHydrate } from '@basenative/runtime';`

#### `hydrateOnIdle(hydrateFn)` (function)
Hydrate when the browser is idle. Uses requestIdleCallback where available, falls back to setTimeout.
packages/runtime/src/lazy.js · `import { hydrateOnIdle } from '@basenative/runtime';`

#### `hydrateOnInteraction(element, hydrateFn, events)` (function)
Hydrate on the first user interaction with the element.
packages/runtime/src/lazy.js · `import { hydrateOnInteraction } from '@basenative/runtime';`

#### `hydrateOnMedia(hydrateFn, query)` (function)
Hydrate when a CSS media query matches.
packages/runtime/src/lazy.js · `import { hydrateOnMedia } from '@basenative/runtime';`

#### `createVitalsReporter(options = {})` (function)
createVitalsReporter(options) — aggregated Web Vitals reporter.
packages/runtime/src/vitals.js · `import { createVitalsReporter } from '@basenative/runtime';`

#### `observeLCP(callback)` (function)
Observe Largest Contentful Paint.
packages/runtime/src/vitals.js · `import { observeLCP } from '@basenative/runtime';`

#### `observeFID(callback)` (function)
Observe First Input Delay.
packages/runtime/src/vitals.js · `import { observeFID } from '@basenative/runtime';`

#### `observeCLS(callback)` (function)
Observe Cumulative Layout Shift.
packages/runtime/src/vitals.js · `import { observeCLS } from '@basenative/runtime';`

#### `observeFCP(callback)` (function)
Observe First Contentful Paint.
packages/runtime/src/vitals.js · `import { observeFCP } from '@basenative/runtime';`

#### `observeTTFB(callback)` (function)
Observe Time to First Byte.
packages/runtime/src/vitals.js · `import { observeTTFB } from '@basenative/runtime';`

#### `observeINP(callback)` (function)
Observe Interaction to Next Paint.
packages/runtime/src/vitals.js · `import { observeINP } from '@basenative/runtime';`

#### `enableDebug(opts = {})` (function)
Enable debug mode.
packages/runtime/src/debug.js · `import { enableDebug } from '@basenative/runtime';`

#### `disableDebug()` (function)
Disable debug mode and reset stats.
packages/runtime/src/debug.js · `import { disableDebug } from '@basenative/runtime';`

#### `isDebugEnabled()` (function)
Returns whether debug mode is active.
packages/runtime/src/debug.js · `import { isDebugEnabled } from '@basenative/runtime';`

#### `getDebugStats()` (function)
Returns accumulated debug statistics.
packages/runtime/src/debug.js · `import { getDebugStats } from '@basenative/runtime';`

#### `debugSignal(s, name = 'signal')` (function)
Wraps a signal with debug logging.
packages/runtime/src/debug.js · `import { debugSignal } from '@basenative/runtime';`

#### `debugEffect(fn, name = 'effect')` (function)
Wraps an effect with debug logging — logs execution count and timing.
packages/runtime/src/debug.js · `import { debugEffect } from '@basenative/runtime';`

#### `debugTime(label, fn)` (function)
Logs a timing measurement. Use around operations you want to profile.
packages/runtime/src/debug.js · `import { debugTime } from '@basenative/runtime';`

#### `debugAssert(condition, message)` (function)
Asserts a reactive invariant in debug mode. No-op in production.
packages/runtime/src/debug.js · `import { debugAssert } from '@basenative/runtime';`

#### `debugDeps(s, name = 'signal')` (function)
Logs a signal's current dependency graph (immediate subscribers only). Useful for diagnosing why a computed is or isn't re-evaluating.
packages/runtime/src/debug.js · `import { debugDeps } from '@basenative/runtime';`

#### `raw(value)` (function)
Mark a string as trusted markup, exempt from HTML escaping.
packages/runtime/src/shared/escape.js · `import { raw } from '@basenative/runtime';`

#### `isScopeSlot(value)` (function)
packages/runtime/src/shared/expression.js · `import { isScopeSlot } from '@basenative/runtime';`

#### `compileExpression(source)` (function)
packages/runtime/src/shared/expression.js · `import { compileExpression } from '@basenative/runtime';`

#### `evaluateExpression(source, ctx = {}, options = {})` (function)
packages/runtime/src/shared/expression.js · `import { evaluateExpression } from '@basenative/runtime';`

#### `clearExpressionCache()` (function)
packages/runtime/src/shared/expression.js · `import { clearExpressionCache } from '@basenative/runtime';`

#### `SCOPE_SLOT` (const)
packages/runtime/src/shared/expression.js · `import { SCOPE_SLOT } from '@basenative/runtime';`

#### `isRaw(value)` (function)
packages/runtime/src/shared/escape.js · `import { isRaw } from '@basenative/runtime';`

#### `unwrapRaw(value)` (function)
The underlying string of a raw value, or the value unchanged.
packages/runtime/src/shared/escape.js · `import { unwrapRaw } from '@basenative/runtime';`

#### `escapeText(value)` (function)
Escape for a text node: `&` first, or the later replacements double-encode.
packages/runtime/src/shared/escape.js · `import { escapeText } from '@basenative/runtime';`

#### `escapeAttr(value)` (function)
Escape for a double-quoted attribute value.
packages/runtime/src/shared/escape.js · `import { escapeAttr } from '@basenative/runtime';`

#### `isUrlAttribute(name)` (function)
packages/runtime/src/shared/escape.js · `import { isUrlAttribute } from '@basenative/runtime';`

#### `sanitizeUrl(value)` (function)
Neutralise a URL-bearing attribute value, or return it unchanged.
packages/runtime/src/shared/escape.js · `import { sanitizeUrl } from '@basenative/runtime';`

#### `findInterpolations(text)` (function)
Locate every `{{ expression }}` in `text`, linearly.
packages/runtime/src/shared/escape.js · `import { findInterpolations } from '@basenative/runtime';`

### `@basenative/server` (v0.4.2)

Server-side rendering with streaming support for BaseNative templates

Source: `packages/server/src` · Docs: `docs/api/server.md`

### `render(html, ctx?, options?)`
Renders an HTML template string with the given context. Processes all BaseNative directives server-side.
```js
import { render } from '@basenative/server';

const html = render(`
  <h1>{{ title }}</h1>
  <template @for="item of items; track item.id">
    <p>{{ item.name }}</p>
  </template>
`, {
  title: 'My Page',
  items: [
…
```

#### Options
| Option | Type | Description | |--------|------|-------------| | `hydratable` | `boolean` | Emit `<!--bn:...-->` marker comments for hydration diagnostics | | `onDiagnostic` | `(diagnostic) => void` | Callback for diagnostic events during…

#### Undocumented exports (source-only — not in docs/api/server.md)

#### `resolveDeferred(options)` (function)
packages/server/src/render.js · `import { resolveDeferred } from '@basenative/server';`

#### `renderToStream(html, ctx, stream, options = {})` (function)
Streaming SSR renderer for BaseNative.
packages/server/src/stream.js · `import { renderToStream } from '@basenative/server';`

#### `renderToReadableStream(html, ctx, options = {})` (function)
Renders a template and returns a Web ReadableStream. Deferred sections are appended after the main content as script injections.
packages/server/src/stream.js · `import { renderToReadableStream } from '@basenative/server';`

### `@basenative/share` (v1.0.0)

Browser-side share helpers.

Source: `packages/share/src`

- `./migrations/0001` → `./migrations/0001_share_cards.sql` (non-JS resource export)

#### `nativeShare(payload)` (async function)
Try the Web Share API; fall back to clipboard; otherwise fail.
packages/share/src/client.js · `import { nativeShare } from '@basenative/share';`

#### `mintShareCard(payload, opts = {})` (async function)
POST a share-card payload and return the minted `{ id, url }`.
packages/share/src/client.js · `import { mintShareCard } from '@basenative/share';`

#### `composeShareText(template, vars = {})` (function)
Tiny `${var}` template helper. Placeholders that don't have a matching key are left untouched (which keeps malformed templates from silently losing data).
packages/share/src/client.js · `import { composeShareText } from '@basenative/share';`

#### `shortId(len = DEFAULT_ID_LENGTH, alphabet = DEFAULT_ALPHABET)` (function)
Generate a cryptographically-random short id.
packages/share/src/server.js · `import { shortId } from '@basenative/share';`

#### `defineShareCards(cfg = {})` (function)
Define a share-card store bound to a DB and a table.
packages/share/src/server.js · `import { defineShareCards } from '@basenative/share';`

#### `mintHandler(opts)` (function)
POST handler for `/api/share-cards`.
packages/share/src/server.js · `import { mintHandler } from '@basenative/share';`

#### `landingHandler(opts)` (function)
GET handler for `/s/{id}` — emits the OG-bearing landing HTML.
packages/share/src/server.js · `import { landingHandler } from '@basenative/share';`

#### `DEFAULT_ALPHABET` (const)
Default short-id alphabet — Crockford-flavoured, no 0/1/i/l/o.
packages/share/src/server.js · `import { DEFAULT_ALPHABET } from '@basenative/share';`

#### `DEFAULT_ID_LENGTH` (const)
packages/share/src/server.js · `import { DEFAULT_ID_LENGTH } from '@basenative/share';`

#### `escHtml(s)` (function)
Defensive HTML escape. Inlined so the module has no dependencies.
packages/share/src/og-redirect.js · `import { escHtml } from '@basenative/share';`

#### `buildLandingHtml(meta)` (function)
Build the full HTML document for a share landing page.
packages/share/src/og-redirect.js · `import { buildLandingHtml } from '@basenative/share';`

### `@basenative/station` (v0.2.0)

@basenative/station — queue-driven local-inference primitive.

Source: `packages/station/src`

#### `ops` (namespace)
Namespace re-export of ./ops.js.
packages/station/src/index.js
  #### `tunnelHealth(url, { fetch = globalThis.fetch, timeoutMs = DEFAULT_TIMEOUT_MS } = {})` (async function)
  tunnelHealth — HTTP GET <url>/health. Returns `{ ok: boolean, status?: number, latencyMs?: number, error?: string }`.
  packages/station/src/ops.js · `import { tunnelHealth } from '@basenative/station';`
  #### `modelHealth(client)` (async function)
  modelHealth — verifies that the expected model is listed at the primary endpoint.
  packages/station/src/ops.js · `import { modelHealth } from '@basenative/station';`
  #### `gpuHealth(url, { fetch = globalThis.fetch, timeoutMs = DEFAULT_TIMEOUT_MS } = {})` (async function)
  gpuHealth — optional remote GPU stat probe. Stubbed; production deployments expose a `/gpu` endpoint over the tunnel that returns `{ tempC, memUsedPct }`.
  packages/station/src/ops.js · `import { gpuHealth } from '@basenative/station';`
  #### `queueHealth(queue)` (function)
  queueHealth — depth, oldest queued age, in-flight count. Pure — runs against an in-process queue instance.
  packages/station/src/ops.js · `import { queueHealth } from '@basenative/station';`
  #### `summary({ tunnelUrl, client, gpuUrl, queue })` (async function)
  summary — composite check used by `bn-station ops`.
  packages/station/src/ops.js · `import { summary } from '@basenative/station';`

#### `Runner` (class)
packages/station/src/runner.js

#### `runOnce({ client, queue, templates, job, applyDiff, log })` (async function)
packages/station/src/runner.js · `import { runOnce } from '@basenative/station';`

#### `run({ client, queue, templates, applyDiff, log, ...opts })` (async function)
packages/station/src/runner.js · `import { run } from '@basenative/station';`

#### `escalate({ queue, job, reason })` (function)
packages/station/src/runner.js · `import { escalate } from '@basenative/station';`

#### `Queue` (class)
Queue — facade over a driver.
packages/station/src/queue.js

#### `createQueue({ path = ':memory:', betterSqlite3 } = {})` (function)
createQueue — driver auto-selection. Always returns a memory driver unless a `betterSqlite3` instance is supplied or one can be loaded.
packages/station/src/queue.js · `import { createQueue } from '@basenative/station';`

#### `MemoryQueueDriver` (class)
In-memory driver — full-featured, used in tests + as the fallback when better-sqlite3 isn't available.
packages/station/src/queue.js

#### `Client` (const)
packages/station/src/client.js · `import { Client } from '@basenative/station';`

#### `OpenAICompatClient` (class)
packages/station/src/client.js

#### `StationUnavailable` (class)
packages/station/src/client.js

#### `StationTimeout` (class)
packages/station/src/client.js

#### `templates` (const)
packages/station/src/templates/index.js · `import { templates } from '@basenative/station';`

#### `defineStation(opts = {})` (function)
packages/station/src/define-station.js · `import { defineStation } from '@basenative/station';`

#### `QUEUE_STATUSES` (const)
packages/station/src/queue.js · `import { QUEUE_STATUSES } from '@basenative/station';`

#### `testsFromTodos` (const)
tests-from-todos — given a file with `// TODO: test X` markers, generate one test per iteration. Loop pattern: knock off one TODO, mark done, advance.
packages/station/src/templates/tests-from-todos.js · `import { testsFromTodos } from '@basenative/station';`

#### `docstringCoverage` (const)
docstring-coverage — add a JSDoc comment to one undocumented export.
packages/station/src/templates/docstring-coverage.js · `import { docstringCoverage } from '@basenative/station';`

#### `lintBankruptcy` (const)
lint-bankruptcy — fix one lint warning per iteration.
packages/station/src/templates/lint-bankruptcy.js · `import { lintBankruptcy } from '@basenative/station';`

#### `refactorMigration` (const)
refactor-migration — apply one mechanical refactor step per iteration.
packages/station/src/templates/refactor-migration.js · `import { refactorMigration } from '@basenative/station';`

#### `fsmClassifier` (const)
fsm-classifier — Greenput intake classification.
packages/station/src/templates/fsm-classifier.js · `import { fsmClassifier } from '@basenative/station';`

#### `tunnelHealth(url, { fetch = globalThis.fetch, timeoutMs = DEFAULT_TIMEOUT_MS } = {})` (async function)
tunnelHealth — HTTP GET <url>/health. Returns `{ ok: boolean, status?: number, latencyMs?: number, error?: string }`.
packages/station/src/ops.js · `import { tunnelHealth } from '@basenative/station';`

#### `modelHealth(client)` (async function)
modelHealth — verifies that the expected model is listed at the primary endpoint.
packages/station/src/ops.js · `import { modelHealth } from '@basenative/station';`

#### `gpuHealth(url, { fetch = globalThis.fetch, timeoutMs = DEFAULT_TIMEOUT_MS } = {})` (async function)
gpuHealth — optional remote GPU stat probe. Stubbed; production deployments expose a `/gpu` endpoint over the tunnel that returns `{ tempC, memUsedPct }`.
packages/station/src/ops.js · `import { gpuHealth } from '@basenative/station';`

#### `queueHealth(queue)` (function)
queueHealth — depth, oldest queued age, in-flight count. Pure — runs against an in-process queue instance.
packages/station/src/ops.js · `import { queueHealth } from '@basenative/station';`

#### `summary({ tunnelUrl, client, gpuUrl, queue })` (async function)
summary — composite check used by `bn-station ops`.
packages/station/src/ops.js · `import { summary } from '@basenative/station';`

### `@basenative/tenant` (v0.3.0)

Multi-tenant middleware with subdomain/path resolution and row-level query scoping

Source: `packages/tenant/src` · Docs: `docs/api/tenant.md`

### API Reference

#### createSubdomainResolver(options)
Resolves tenant from the request hostname's leading subdomain. **Parameters:** - `options.baseDomain` — the base domain to strip (e.g. `'example.com'`); if omitted, assumes the last two segments are the base domain - `options.exclude` — su…
```
acme.example.com  → 'acme'
www.example.com   → null (excluded)
example.com       → null (no subdomain)
```

#### createPathResolver(options)
Resolves tenant from a URL path prefix. **Parameters:** - `options.prefix` — path prefix segment; default `'/t'` **Returns:** Resolver function `(ctx) => string | null`. **Example:**
```
/t/acme/users  → 'acme'
/t/acme        → 'acme'
/dashboard     → null
```

#### createHeaderResolver(options)
Resolves tenant from a request header. **Parameters:** - `options.header` — header name; default `'x-tenant-id'` **Returns:** Resolver function `(ctx) => string | null`.

#### createCompositeResolver(resolvers)
Tries multiple resolvers in order and returns the first non-null result. **Parameters:** - `resolvers` — array of resolver functions (at least one required) **Returns:** Resolver function `(ctx) => string | null`. **Example:**
```js
const resolver = createCompositeResolver([
  createSubdomainResolver({ baseDomain: 'example.com' }),
  createHeaderResolver({ header: 'x-tenant-id' }),
]);
```

#### tenantMiddleware(resolver, options)
Middleware that runs the resolver and stores the result in `ctx.state`. **Parameters:** - `resolver` — any resolver function - `options.stateKey` — key to store the tenant under in `ctx.state`; default `'tenant'` - `options.onNotFound(ctx)…

#### requireTenant(options)
Middleware that blocks requests where no tenant was resolved. **Parameters:** - `options.stateKey` — key to read in `ctx.state`; default `'tenant'` - `options.status` — HTTP status for rejection; default `400` - `options.message` — error m…

#### tenantScope(adapter, options)
Wraps a database adapter to automatically add the `tenant_id` column to every query. **Parameters:** - `adapter` — database adapter with `query`, `insert`, `update`, `delete` methods - `options.column` — tenant column name; default `'tenan…
```js
const scopedDb = tenantScope(db, { column: 'org_id' });

// In a route handler (ctx.state.tenant = 'acme'):
await scopedDb.insert(ctx, 'projects', { name: 'Alpha' });
// Inserts: { name: 'Alpha', org_id: 'acme' }

const projects = await scopedDb.query(ctx, 'projects');
// Queries: SELECT * FROM projects WHERE org_id = 'acme'
```

### `@basenative/tsconfig` (v0.2.0)

Shared base tsconfigs for DuganLabs projects — strict, modern, runtime-targeted

Source: `packages/tsconfig/src`

- `.` → `./base.json` (non-JS resource export)
- `./base` → `./base.json` (non-JS resource export)
- `./browser` → `./browser.json` (non-JS resource export)
- `./node` → `./node.json` (non-JS resource export)
- `./worker` → `./worker.json` (non-JS resource export)
- `./react` → `./react.json` (non-JS resource export)

_(no JS exports found — resource-only or config-only package; see exports map above.)_

### `@basenative/upload` (v0.3.0)

File upload handling with R2 and S3 adapter support

Source: `packages/upload/src` · Docs: `docs/api/upload.md`

### API Reference

#### createUploadHandler(storage, options)
Creates an upload middleware that parses multipart requests and stores files. **Parameters:** - `storage` — storage adapter instance (local, S3, or R2) - `options.maxFileSize` — maximum bytes per file; default `10485760` (10 MB) - `options…

#### parseMultipart(body, contentType)
Parses a raw multipart request body into parts. **Parameters:** - `body` — request body as `Buffer` or string - `contentType` — value of the `Content-Type` header (must contain `boundary=`) **Returns:** Array of part objects: `{ name, file…
```js
const parts = parseMultipart(req.body, req.headers['content-type']);
const files = parts.filter(p => p.filename);
const fields = parts.filter(p => !p.filename);
```

#### createLocalStorage(options)
Storage adapter that writes files to the local filesystem. **Parameters:** - `options.directory` — directory path for uploaded files - `options.baseUrl` — URL prefix for constructing file URLs; default `'/uploads'` **Returns:** Storage ada…

#### createS3Storage(options)
Storage adapter for AWS S3 or any S3-compatible service (MinIO, DigitalOcean Spaces, etc.). **Parameters:** - `options.client` — an AWS SDK v3 S3 client instance (required) - `options.bucket` — S3 bucket name (required) - `options.prefix`…
```js
import { S3Client } from '@aws-sdk/client-s3';
import { createS3Storage } from '@basenative/upload';

const storage = createS3Storage({
  client: new S3Client({ region: 'us-east-1' }),
  bucket: 'my-uploads',
  prefix: 'avatars',
});
```

#### createR2Storage(options)
Storage adapter for Cloudflare R2. Accepts a Cloudflare Workers R2 bucket binding. **Parameters:** - `options.bucket` — R2 bucket binding from the Workers environment (required) - `options.prefix` — key prefix; default `''` - `options.base…

### Storage Adapter Interface
All storage adapters implement the same interface:
```js
{
  put(filename, data, metadata)  // => Promise<{ key, url, size, contentType }>
  get(filename)                  // => Promise<Buffer | ReadableStream>
  delete(filename)               // => Promise<void>
  exists(filename)               // => Promise<boolean>
}
```

### `@basenative/validate` (v0.1.0)

Structured validation for BaseNative templates — machine-actionable errors a model can repair from the error object alone

Source: `packages/validate/src`

#### `validateTemplate(source, options = {})` (function)
Validate a BaseNative template.
packages/validate/src/validate.js · `import { validateTemplate } from '@basenative/validate';`

#### `CODES` (const)
packages/validate/src/codes.js · `import { CODES } from '@basenative/validate';`

#### `ERROR` (const)
Blocking: the template will not render, or will render wrongly and silently.
packages/validate/src/codes.js · `import { ERROR } from '@basenative/validate';`

#### `WARNING` (const)
Non-blocking: renders today, but relies on behaviour that is unspecified or fragile.
packages/validate/src/codes.js · `import { WARNING } from '@basenative/validate';`

#### `CONFIDENCE` (const)
`high` — the correction is mechanical; a model can apply it verbatim. `medium` — the correction is the common case but depends on author intent. `low` — the diagnostic is a strong signal, but the fix requires context we lack.
packages/validate/src/codes.js · `import { CONFIDENCE } from '@basenative/validate';`

### `@basenative/visual-builder` (v0.2.0)

No-code template builder for BaseNative components

Source: `packages/visual-builder/src` · Docs: `docs/api/visual-builder.md`

### API Reference

#### createCanvas(options)
Creates a new builder canvas. **Options:** - `width` — canvas width in pixels (default: `1024`) - `height` — canvas height in pixels (default: `768`) - `gridSize` — snap grid size in pixels (default: `8`) **Returns:** `Canvas` with: - `add…

#### renderCanvas(canvas)
Renders all canvas nodes to an HTML string using `@basenative/server`. **Returns:** `string` — HTML

#### renderNode(node)
Renders a single canvas node to HTML. **Returns:** `string` — HTML fragment

#### serialize(canvas)
Serializes canvas state to a JSON string. Version-stamped for future migrations. **Returns:** `string` — JSON

#### deserialize(json)
Loads a serialized JSON string back into a new `Canvas` instance. **Returns:** `Canvas`

#### exportToHTML(canvas)
Exports the canvas as a standalone BaseNative HTML template string suitable for use with `@basenative/server`. **Returns:** `string` — HTML template

#### importFromHTML(html)
Parses a BaseNative HTML template and reconstructs a canvas from it. **Returns:** `Canvas`

#### createComponentPalette(options)
Creates a component palette for a drag-and-drop UI. **Options:** - `components` — array of component definitions to offer in the palette **Returns:** `Palette` with: - `getComponents()` — get all palette components - `getComponent(type)` —…

#### Undocumented exports (source-only — not in docs/api/visual-builder.md)

#### `BnCanvas` (class)
<bn-canvas> Web Component
packages/visual-builder/src/bn-canvas.js

### `@basenative/wrangler-preset` (v0.2.0)

Recommended baseline for any DuganLabs Worker / Pages project.

Source: `packages/wrangler-preset/src`

#### `mergeWrangler(base, ...frags)` (function)
Merge a base config and any number of fragments into a single wrangler config object. Array-valued bindings (d1_databases, kv_namespaces, etc.) are concatenated; object-valued bindings (durable_objects.bindings) are merged deeply by the sa…
packages/wrangler-preset/src/index.js · `import { mergeWrangler } from '@basenative/wrangler-preset';`

#### `toToml(config)` (function)
Serialize a wrangler config object to TOML.
packages/wrangler-preset/src/index.js · `import { toToml } from '@basenative/wrangler-preset';`

#### `defaults` (const)
Recommended baseline for any DuganLabs Worker / Pages project.
packages/wrangler-preset/src/index.js · `import { defaults } from '@basenative/wrangler-preset';`

#### `bindings` (const)
Fragment builders. Each returns a plain object that can be merged into a wrangler config via `mergeWrangler`.
packages/wrangler-preset/src/index.js · `import { bindings } from '@basenative/wrangler-preset';`

## Gaps

Everything below is a real, mechanically-detected gap between source/package.json and docs — not inferred behavior. Fix by writing the missing doc, then re-running this generator (it will pick the doc up automatically).

- Directive `@defer` is read via getAttribute/hasAttribute in packages/runtime/src/hydrate.js and/or packages/server/src/render.js but is not covered by docs/api/runtime.md's "Template Directives" section or docs/api/server.md's server-side-directives section.
- `@basenative/admin` — no docs/api/admin.md; API below is extracted directly from source signatures/JSDoc only (13 exports).
- `@basenative/auth-webauthn` — no docs/api/auth-webauthn.md; API below is extracted directly from source signatures/JSDoc only (30 exports).
- `@basenative/builder` — no docs/api/builder.md; API below is extracted directly from source signatures/JSDoc only (16 exports).
- `@basenative/claude-config` — no docs/api/claude-config.md; API below is extracted directly from source signatures/JSDoc only (7 exports).
- `@basenative/combobox` — no docs/api/combobox.md; API below is extracted directly from source signatures/JSDoc only (10 exports).
- `@basenative/components`: `buttonVariants` is exported (packages/components/src/button.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderCombobox` is exported (packages/components/src/combobox.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderMultiselect` is exported (packages/components/src/multiselect.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderDataGrid` is exported (packages/components/src/datagrid.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderTree` is exported (packages/components/src/tree.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderTreeGrid` is exported (packages/components/src/tree.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderVirtualList` is exported (packages/components/src/virtualizer.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderDialog` is exported (packages/components/src/dialog.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderDrawer` is exported (packages/components/src/drawer.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderTabs` is exported (packages/components/src/tabs.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderAccordion` is exported (packages/components/src/accordion.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderBreadcrumb` is exported (packages/components/src/breadcrumb.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderAvatar` is exported (packages/components/src/avatar.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderTooltip` is exported (packages/components/src/tooltip.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderDropdownMenu` is exported (packages/components/src/dropdown-menu.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderCommandPalette` is exported (packages/components/src/command-palette.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderCalendar` is exported (packages/components/src/calendar.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderPipelineBlock` is exported (packages/components/src/calendar.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderPipeline` is exported (packages/components/src/calendar.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `initCalendarDragDrop` is exported (packages/components/src/calendar.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `initPipelineDragDrop` is exported (packages/components/src/calendar.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `createCalendarState` is exported (packages/components/src/calendar-state.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `createPipelineState` is exported (packages/components/src/calendar-state.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `eventsCollide` is exported (packages/components/src/calendar-state.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `updateEvent` is exported (packages/components/src/calendar-state.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `getEventDuration` is exported (packages/components/src/calendar-state.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `renderLayoutGrid` is exported (packages/components/src/layout-grid.js) but not mentioned in docs/api/components.md.
- `@basenative/components`: `layoutGridStyles` is exported (packages/components/src/layout-grid.js) but not mentioned in docs/api/components.md.
- `@basenative/doppler` — no docs/api/doppler.md; API below is extracted directly from source signatures/JSDoc only (8 exports).
- `@basenative/favicon` — no docs/api/favicon.md; API below is extracted directly from source signatures/JSDoc only (33 exports).
- `@basenative/flags`: `createKVProvider` is exported (packages/flags/src/providers/kv.js) but not mentioned in docs/api/flags.md.
- `@basenative/flags`'s package.json description advertises a `@feature` template directive, but no directive registration (`addDirective`/literal `'@feature'` attribute handling) exists in packages/flags/src — not documented here since it isn't in source.
- `@basenative/forms`: `createWizard` is exported (packages/forms/src/wizard.js) but not mentioned in docs/api/forms.md.
- `@basenative/i18n`'s package.json description advertises a `@t` template directive, but no directive registration (`addDirective`/literal `'@t'` attribute handling) exists in packages/i18n/src — not documented here since it isn't in source.
- `@basenative/integrations` — no docs/api/integrations.md; API below is extracted directly from source signatures/JSDoc only (15 exports).
- `@basenative/keyboard` — no docs/api/keyboard.md; API below is extracted directly from source signatures/JSDoc only (12 exports).
- `@basenative/logger`: `LEVELS` is exported (packages/logger/src/index.js) but not mentioned in docs/api/logger.md.
- `@basenative/logger`: `LEVEL_NAMES` is exported (packages/logger/src/index.js) but not mentioned in docs/api/logger.md.
- `@basenative/markdown` — no docs/api/markdown.md; API below is extracted directly from source signatures/JSDoc only (4 exports).
- `@basenative/marketplace`: `renderPackageCard` is exported (packages/marketplace/src/card.js) but not mentioned in docs/api/marketplace.md.
- `@basenative/marketplace`: `packageCardStyles` is exported (packages/marketplace/src/card.js) but not mentioned in docs/api/marketplace.md.
- `@basenative/mcp` — no docs/api/mcp.md; API below is extracted directly from source signatures/JSDoc only (9 exports).
- `@basenative/middleware`: `createExpressContext` is exported (packages/middleware/src/adapters/express.js) but not mentioned in docs/api/middleware.md.
- `@basenative/og-image` — no docs/api/og-image.md; API below is extracted directly from source signatures/JSDoc only (15 exports).
- `@basenative/persist` — no docs/api/persist.md; API below is extracted directly from source signatures/JSDoc only (16 exports).
- `@basenative/runtime`: `batch` is exported (packages/runtime/src/signals.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `registerPlugin` is exported (packages/runtime/src/signals.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `enableDevtools` is exported (packages/runtime/src/devtools.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `disableDevtools` is exported (packages/runtime/src/devtools.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `isDevtoolsEnabled` is exported (packages/runtime/src/devtools.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `trackSignal` is exported (packages/runtime/src/devtools.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `trackEffect` is exported (packages/runtime/src/devtools.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `recordHydration` is exported (packages/runtime/src/devtools.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `createErrorBoundary` is exported (packages/runtime/src/error-boundary.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `renderWithBoundary` is exported (packages/runtime/src/error-boundary.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `definePlugin` is exported (packages/runtime/src/plugins.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `createPluginRegistry` is exported (packages/runtime/src/plugins.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `createLazyHydrator` is exported (packages/runtime/src/lazy.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `lazyHydrate` is exported (packages/runtime/src/lazy.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `hydrateOnIdle` is exported (packages/runtime/src/lazy.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `hydrateOnInteraction` is exported (packages/runtime/src/lazy.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `hydrateOnMedia` is exported (packages/runtime/src/lazy.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `createVitalsReporter` is exported (packages/runtime/src/vitals.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `observeLCP` is exported (packages/runtime/src/vitals.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `observeFID` is exported (packages/runtime/src/vitals.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `observeCLS` is exported (packages/runtime/src/vitals.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `observeFCP` is exported (packages/runtime/src/vitals.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `observeTTFB` is exported (packages/runtime/src/vitals.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `observeINP` is exported (packages/runtime/src/vitals.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `enableDebug` is exported (packages/runtime/src/debug.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `disableDebug` is exported (packages/runtime/src/debug.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `isDebugEnabled` is exported (packages/runtime/src/debug.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `getDebugStats` is exported (packages/runtime/src/debug.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `debugSignal` is exported (packages/runtime/src/debug.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `debugEffect` is exported (packages/runtime/src/debug.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `debugTime` is exported (packages/runtime/src/debug.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `debugAssert` is exported (packages/runtime/src/debug.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `debugDeps` is exported (packages/runtime/src/debug.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `raw` is exported (packages/runtime/src/shared/escape.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `isScopeSlot` is exported (packages/runtime/src/shared/expression.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `compileExpression` is exported (packages/runtime/src/shared/expression.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `evaluateExpression` is exported (packages/runtime/src/shared/expression.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `clearExpressionCache` is exported (packages/runtime/src/shared/expression.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `SCOPE_SLOT` is exported (packages/runtime/src/shared/expression.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `isRaw` is exported (packages/runtime/src/shared/escape.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `unwrapRaw` is exported (packages/runtime/src/shared/escape.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `escapeText` is exported (packages/runtime/src/shared/escape.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `escapeAttr` is exported (packages/runtime/src/shared/escape.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `isUrlAttribute` is exported (packages/runtime/src/shared/escape.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `sanitizeUrl` is exported (packages/runtime/src/shared/escape.js) but not mentioned in docs/api/runtime.md.
- `@basenative/runtime`: `findInterpolations` is exported (packages/runtime/src/shared/escape.js) but not mentioned in docs/api/runtime.md.
- `@basenative/server`: `resolveDeferred` is exported (packages/server/src/render.js) but not mentioned in docs/api/server.md.
- `@basenative/server`: `renderToStream` is exported (packages/server/src/stream.js) but not mentioned in docs/api/server.md.
- `@basenative/server`: `renderToReadableStream` is exported (packages/server/src/stream.js) but not mentioned in docs/api/server.md.
- `@basenative/share` — no docs/api/share.md; API below is extracted directly from source signatures/JSDoc only (11 exports).
- `@basenative/station` — no docs/api/station.md; API below is extracted directly from source signatures/JSDoc only (25 exports).
- `@basenative/validate` — no docs/api/validate.md; API below is extracted directly from source signatures/JSDoc only (5 exports).
- `@basenative/visual-builder`: `BnCanvas` is exported (packages/visual-builder/src/bn-canvas.js) but not mentioned in docs/api/visual-builder.md.
- `@basenative/wrangler-preset` — no docs/api/wrangler-preset.md; API below is extracted directly from source signatures/JSDoc only (4 exports).

_Private packages excluded from this reference: `@basenative/evals`, `@basenative/fonts`, `@basenative/icons`._
