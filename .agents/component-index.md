# BaseNative component index

<!-- GENERATED by scripts/llms-txt.js — do not edit. Run `node scripts/llms-txt.js`. -->

Generated 2026-09-12 · 66 components (60 render functions, 6 custom elements) · 174 styled `data-bn` contracts.

Allowed variants below are read out of the stylesheets that declare them, not out of docstrings: a value outside the listed set does **not** error, it renders with the base rule only and no styling. Every axis also has one unnamed implicit default (the state with no `data-*` attribute).

Machine form, including the `data-bn` → component reverse index: `.agents/component-index.json`.

## `@basenative/admin`

### `renderAdminQueueList({ items, approveLabel, rejectLabel, emptyLabel, actionHandler })`
Render the moderation queue list.
`packages/admin/src/components.js` · emits `<ul data-bn="admin-queue-list">`
- variants: none declared
- **unstyled**: no stylesheet declares `data-bn="admin-queue-list"`
- use instead of: a bare <ul> with your own classes, or literal <ul data-bn="admin-queue-list"> markup

### `renderAdminUserList({ users, results, query, currentHandle, roles, labels, actionHandler, …+1 })`
Render the admin user-management list with search and per-row role controls.
`packages/admin/src/components.js` · emits `<div data-bn="admin-user-list">`
- variants: none declared
- **unstyled**: no stylesheet declares `data-bn="admin-user-list"`
- use instead of: a bare <div> with your own classes, or literal <div data-bn="admin-user-list"> markup

## `@basenative/builder`

### `<bn-builder></bn-builder>`
`packages/builder/src/builder-element.js`
- variants: none declared
- use instead of: a hand-rolled <div> reimplementation of <bn-builder>

### `<bn-builder-canvas></bn-builder-canvas>`
`packages/builder/src/canvas-element.js`
- variants: none declared
- registered more than once: `packages/builder/src/builder-element.js`, `packages/builder/src/canvas-element.js`
- use instead of: a hand-rolled <div> reimplementation of <bn-builder-canvas>

### `<bn-builder-inspector></bn-builder-inspector>`
`packages/builder/src/inspector-element.js`
- variants: none declared
- registered more than once: `packages/builder/src/builder-element.js`, `packages/builder/src/inspector-element.js`
- use instead of: a hand-rolled <div> reimplementation of <bn-builder-inspector>

### `<bn-builder-palette></bn-builder-palette>`
`packages/builder/src/palette-element.js` · emits `<div>`
- variants: none declared
- registered more than once: `packages/builder/src/builder-element.js`, `packages/builder/src/palette-element.js`
- use instead of: a hand-rolled <div> reimplementation of <bn-builder-palette>

### `<bn-builder-tree></bn-builder-tree>`
`packages/builder/src/tree-element.js`
- variants: none declared
- registered more than once: `packages/builder/src/builder-element.js`, `packages/builder/src/tree-element.js`
- use instead of: a hand-rolled <div> reimplementation of <bn-builder-tree>

### `renderInspector(state, palette)`
`packages/builder/src/inspector.js` · emits `<header>`
- variants: none declared
- use instead of: a bare <header> with your own classes

### `renderNodeToElement(doc, node, palette)`
`packages/builder/src/dom-render.js`
- variants: none declared
- use instead of: hand-written markup for this element

### `renderPaletteHTML(palette)`
`packages/builder/src/palette-element.js` · emits `<li>`
- variants: none declared
- use instead of: a bare <li> with your own classes

### `renderTreeView(state)`
`packages/builder/src/tree-view.js` · emits `<ul>`
- variants: none declared
- use instead of: a bare <ul> with your own classes

## `@basenative/combobox`

### `renderCombobox(options)`
Render the combobox as an HTML string — pre-paint friendly. The static markup includes: - root <div role="com…
`packages/combobox/src/combobox.js` · emits `<div data-bn="combobox">` + 6 more contract(s)
- variants: none declared
- use instead of: a bare <div> with your own classes, or literal <div data-bn="combobox"> markup

## `@basenative/components`

### `renderAccordion(options)`
Accordion — collapsible sections using native <details>/<summary>.
`packages/components/src/accordion.js` · emits `<div data-bn="accordion">` + 3 more contract(s)
- variants: none declared
- use instead of: a bare <div> with your own classes, or literal <div data-bn="accordion"> markup

### `renderAlert(content, options)`
Alert component — inline feedback; error/warning use role="alert", others role="status".
`packages/components/src/alert.js` · emits `<div data-bn="alert">` + 2 more contract(s)
- variant: `error|info|success|warning`
- use instead of: a bare <div> with your own classes, or literal <div data-bn="alert"> markup

### `renderAvatar(options)`
Avatar — user avatar with image, initials, or icon fallback.
`packages/components/src/avatar.js` · emits `<span data-bn="avatar">` + 2 more contract(s)
- size: `lg|sm|xl`
- shape: `square`
- use instead of: a bare <span> with your own classes, or literal <span data-bn="avatar"> markup

### `renderBadge(content, options)`
Badge component — status indicator.
`packages/components/src/badge.js` · emits `<span data-bn="badge">`
- variant: `default|error|primary|success|warning`
- use instead of: a bare <span> with your own classes, or literal <span data-bn="badge"> markup

### `renderBreadcrumb(options)`
Breadcrumb — navigation trail; the last item is aria-current="page".
`packages/components/src/breadcrumb.js` · emits `<nav data-bn="breadcrumb">` + 3 more contract(s)
- variants: none declared
- use instead of: a bare <nav> with your own classes, or literal <nav data-bn="breadcrumb"> markup

### `renderButton(content, options)`
Server-side render helper for a button element.
`packages/components/src/button.js` · emits `<button data-bn="button">`
- size: `lg|sm`
- variant: `destructive|ghost|primary|secondary`
- use instead of: a bare <button> with your own classes, or literal <button data-bn="button"> markup

### `renderCalendar(options)`
Server-side render a weekly calendar grid with event blocks.
`packages/components/src/calendar.js` · emits `<div data-bn="calendar">` + 10 more contract(s)
- calendar-day-header.today: `<any value>` — open set, a rule matches beyond the listed values
- calendar-event.dragging: `<any value>` — open set, a rule matches beyond the listed values
- calendar-event.status: `canceled|cancelled|completed|in_progress|invoiced|paid|scheduled`
- calendar-event.continues: `after|before|both`
- calendar-slot.drop-target: `<any value>` — open set, a rule matches beyond the listed values
- calendar-day-column.today: `<any value>` — open set, a rule matches beyond the listed values
- use instead of: a bare <div> with your own classes, or literal <div data-bn="calendar"> markup

### `renderCard(options)`
Card component — semantic <article> container.
`packages/components/src/card.js` · emits `<article data-bn="card">` + 3 more contract(s)
- variants: none declared
- **inert axis** (written to the DOM, no stylesheet reads it for this component): `data-variant`
- use instead of: a bare <article> with your own classes, or literal <article data-bn="card"> markup

### `renderCheckbox(options)`
Checkbox component — native <input type="checkbox"> with label association.
`packages/components/src/checkbox.js` · emits `<input data-bn="checkbox">` + 1 more contract(s)
- variants: none declared
- use instead of: a bare <input> with your own classes, or literal <input data-bn="checkbox"> markup

### `renderCombobox(options)`
Combobox — searchable dropdown with keyboard navigation, rendered as a native <input> + <datalist>. Attribute…
`packages/components/src/combobox.js` · emits `<div data-bn="combobox">` + 1 more contract(s)
- variants: none declared
- use instead of: a bare <div> with your own classes, or literal <div data-bn="combobox"> markup

### `renderCommandPalette(options)`
Command palette — Cmd+K style searchable command menu in a native <dialog>.
`packages/components/src/command-palette.js` · emits `<dialog data-bn="command-palette">` + 10 more contract(s)
- variants: none declared
- use instead of: a bare <dialog> with your own classes, or literal <dialog data-bn="command-palette"> markup

### `renderDataGrid(options)`
Data grid — sortable, filterable, paginated table with virtual scrolling support.
`packages/components/src/datagrid.js` · emits `<div data-bn="datagrid">` + 13 more contract(s)
- datagrid-th.sortable: `<any value>` — open set, a rule matches beyond the listed values
- datagrid-th.sorted: `asc|desc` — open set, a rule matches beyond the listed values
- use instead of: a bare <div> with your own classes, or literal <div data-bn="datagrid"> markup

### `renderDialog(options)`
Dialog — modal/non-modal dialog using native <dialog> element.
`packages/components/src/dialog.js` · emits `<dialog data-bn="dialog">` + 3 more contract(s)
- size: `lg|sm`
- use instead of: a bare <dialog> with your own classes, or literal <dialog data-bn="dialog"> markup

### `renderDrawer(options)`
Drawer — side panel that slides in from the edge.
`packages/components/src/drawer.js` · emits `<aside data-bn="drawer">` + 4 more contract(s)
- drawer-overlay.open: `<any value>` — open set, a rule matches beyond the listed values
- position: `left`
- open: `<any value>` — open set, a rule matches beyond the listed values
- size: `lg|sm`
- use instead of: a bare <aside> with your own classes, or literal <aside data-bn="drawer"> markup

### `renderDropdownMenu(options)`
Dropdown menu — popover-based menu with role="menu" / "menuitem".
`packages/components/src/dropdown-menu.js` · emits `<div data-bn="dropdown-menu">` + 6 more contract(s)
- position: `left|right <position$="end"> <position^="top">` — open set, a rule matches beyond the listed values
- use instead of: a bare <div> with your own classes, or literal <div data-bn="dropdown-menu"> markup

### `renderInput(options)`
Server-side render helper for an input field group.
`packages/components/src/input.js` · emits `<input data-bn="input">`
- variants: none declared
- use instead of: a bare <input> with your own classes, or literal <input data-bn="input"> markup

### `renderLayoutGrid(options)`
renderLayoutGrid - A drag-and-drop CSS grid layout component. Designed to hook into @basenative/visual-builde…
`packages/components/src/layout-grid.js` · emits `<div data-bn="layout-grid">` + 1 more contract(s)
- variants: none declared
- use instead of: a bare <div> with your own classes, or literal <div data-bn="layout-grid"> markup

### `renderMultiselect(options)`
Multiselect — multiple value selection with tags/chips over a hidden native <select multiple>. Attributes, la…
`packages/components/src/multiselect.js` · emits `<div data-bn="multiselect">` + 5 more contract(s)
- disabled: `<any value>` — open set, a rule matches beyond the listed values
- use instead of: a bare <div> with your own classes, or literal <div data-bn="multiselect"> markup

### `renderPagination(options)`
Renders a pagination control. Page link hrefs are escaped attributes.
`packages/components/src/pagination.js` · emits `<nav data-bn="pagination">` + 1 more contract(s)
- variants: none declared
- use instead of: a bare <nav> with your own classes, or literal <nav data-bn="pagination"> markup

### `renderPipeline(options)`
Render a kanban-style pipeline view with draggable columns and cards.
`packages/components/src/calendar.js` · emits `<div data-bn="pipeline">` + 5 more contract(s)
- pipeline-column-cards.drop-target: `<any value>` — open set, a rule matches beyond the listed values
- use instead of: a bare <div> with your own classes, or literal <div data-bn="pipeline"> markup

### `renderPipelineBlock(options)`
Render a pipeline/kanban block for use outside the calendar (e.g., sidebar cards that can be dragged onto the…
`packages/components/src/calendar.js` · emits `<div data-bn="pipeline-block">` + 1 more contract(s)
- dragging: `<any value>` — open set, a rule matches beyond the listed values
- status: `accepted|active|canceled|cancelled|completed|contacted|converted|declined|in_progress|invoiced|lost|negotiating|new|open|paid|paused|pending|proposal_sent|qualified|rejected|scheduled|sent|won` — open set, a rule matches beyond the listed values
- use instead of: a bare <div> with your own classes, or literal <div data-bn="pipeline-block"> markup

### `renderProgress(options)`
Renders a determinate progress bar using native <progress>. The fallback percentage text is 0% when max is 0,…
`packages/components/src/progress.js` · emits `<progress data-bn="progress">`
- variants: none declared
- use instead of: a bare <progress> with your own classes, or literal <progress data-bn="progress"> markup

### `renderRadioGroup(options)`
Radio component — native <input type="radio"> group inside a <fieldset>.
`packages/components/src/radio.js` · emits `<fieldset data-bn="radio-group">` + 2 more contract(s)
- variants: none declared
- use instead of: a bare <fieldset> with your own classes, or literal <fieldset data-bn="radio-group"> markup

### `renderSelect(options)`
Server-side render helper for a select field group.
`packages/components/src/select.js` · emits `<select data-bn="select">`
- variants: none declared
- use instead of: a bare <select> with your own classes, or literal <select data-bn="select"> markup

### `renderSkeleton(options)`
Renders `count` skeleton placeholders.
`packages/components/src/skeleton.js` · emits `<div data-bn="skeleton">`
- variant: `circle`
- use instead of: a bare <div> with your own classes, or literal <div data-bn="skeleton"> markup

### `renderSpinner(options)`
Renders an indeterminate spinner.
`packages/components/src/progress.js` · emits `<span data-bn="spinner">`
- size: `lg|sm`
- use instead of: a bare <span> with your own classes, or literal <span data-bn="spinner"> markup

### `renderTable(options)`
Renders a data table from columns and rows. Column labels, cell values, caption and emptyMessage are escaped…
`packages/components/src/table.js` · emits `<table data-bn="table">` + 3 more contract(s)
- variants: none declared
- use instead of: a bare <table> with your own classes, or literal <table data-bn="table"> markup

### `renderTabs(options)`
Tabs — tab navigation with panels. Tab buttons are type="button" and wired to their panels with aria-controls…
`packages/components/src/tabs.js` · emits `<div data-bn="tabs">` + 3 more contract(s)
- variant: `pills`
- use instead of: a bare <div> with your own classes, or literal <div data-bn="tabs"> markup

### `renderTextarea(options)`
Server-side render helper for a textarea field group.
`packages/components/src/textarea.js` · emits `<textarea data-bn="textarea">`
- variants: none declared
- use instead of: a bare <textarea> with your own classes, or literal <textarea data-bn="textarea"> markup

### `renderToastContainer(position)`
Server-side render helper for toast container.
`packages/components/src/toast.js` · emits `<div data-bn="toast-container">`
- position: `bottom-left|bottom-right|top-left|top-right`
- use instead of: a bare <div> with your own classes, or literal <div data-bn="toast-container"> markup

### `renderToggle(options)`
Toggle/Switch component — <input type="checkbox" role="switch">.
`packages/components/src/toggle.js` · emits `<input data-bn="toggle">` + 1 more contract(s)
- variants: none declared
- use instead of: a bare <input> with your own classes, or literal <input data-bn="toggle"> markup

### `renderTooltip(options)`
Tooltip — popover-based tooltip using the Popover API.
`packages/components/src/tooltip.js` · emits `<span data-bn="tooltip">`
- position: `bottom|left|right`
- use instead of: a bare <span> with your own classes, or literal <span data-bn="tooltip"> markup

### `renderTree(options)`
Tree view — expandable hierarchical tree.
`packages/components/src/tree.js` · emits `<ul data-bn="tree">` + 7 more contract(s)
- tree-item.level: `1|2|3`
- tree-item-content.selected: `<any value>` — open set, a rule matches beyond the listed values
- use instead of: a bare <ul> with your own classes, or literal <ul data-bn="tree"> markup

### `renderTreeGrid(options)`
TreeGrid — tree structure combined with table columns.
`packages/components/src/tree.js` · emits `<table data-bn="treegrid">` + 1 more contract(s)
- variants: none declared
- **inert axis** (written to the DOM, no stylesheet reads it for this component): `data-level`
- use instead of: a bare <table> with your own classes, or literal <table data-bn="treegrid"> markup

### `renderVirtualList(options)`
Virtualizer — renders a virtual scroll container placeholder: a window of items plus a spacer for total heigh…
`packages/components/src/virtualizer.js` · emits `<div data-bn="virtualizer">` + 3 more contract(s)
- variants: none declared
- use instead of: a bare <div> with your own classes, or literal <div data-bn="virtualizer"> markup

## `@basenative/date`

### `renderDatepicker(options)`
Date picker using native <input type="date"> with progressive enhancement.
`packages/date/src/datepicker.js` · emits `<div data-bn="datepicker">` + 1 more contract(s)
- variants: none declared
- **unstyled**: no stylesheet declares `data-bn="datepicker"`
- use instead of: a bare <div> with your own classes, or literal <div data-bn="datepicker"> markup

### `renderDateRange(options)`
Date range picker — two date inputs for start/end.
`packages/date/src/daterange.js` · emits `<fieldset data-bn="daterange">` + 5 more contract(s)
- variants: none declared
- **unstyled**: no stylesheet declares `data-bn="daterange"`
- use instead of: a bare <fieldset> with your own classes, or literal <fieldset data-bn="daterange"> markup

### `renderTimepicker(options)`
Time picker using native <input type="time">.
`packages/date/src/timepicker.js` · emits `<div data-bn="timepicker">` + 1 more contract(s)
- variants: none declared
- **unstyled**: no stylesheet declares `data-bn="timepicker"`
- use instead of: a bare <div> with your own classes, or literal <div data-bn="timepicker"> markup

## `@basenative/favicon`

### `renderAppleSvg(spec)`
Render an apple-touch-icon SVG — Apple wants a non-transparent square, no rounded corners (iOS rounds them it…
`packages/favicon/src/render.js`
- variants: none declared
- use instead of: hand-written markup for this element

### `renderFaviconSvg(spec)`
Render a complete favicon SVG document.
`packages/favicon/src/render.js` · emits `<svg>`
- variants: none declared
- use instead of: a bare <svg> with your own classes

### `renderGlyph(kind, spec, palette)`
Dispatch a glyph by `kind`.
`packages/favicon/src/glyphs.js`
- variants: none declared
- use instead of: hand-written markup for this element

### `renderMaskableSvg(spec)`
Render a maskable variant — adds the 80% safe zone Android requires by scaling the glyph down (the body fills…
`packages/favicon/src/render.js` · emits `<svg>`
- variants: none declared
- use instead of: a bare <svg> with your own classes

## `@basenative/keyboard`

### `renderKeyboard(options)`
Render the keyboard as an HTML string. Pre-paint friendly — works in SSR, hydrates cleanly client-side.
`packages/keyboard/src/keyboard.js` · emits `<div data-bn="keyboard">` + 3 more contract(s)
- variants: none declared
- use instead of: a bare <div> with your own classes, or literal <div data-bn="keyboard"> markup

## `@basenative/marketplace`

### `renderPackageCard(pkg)`
Renders a marketplace package card as an HTML string. Works on both server (SSR) and client via @basenative/c…
`packages/marketplace/src/card.js` · emits `<article data-bn="pkg-card">` + 4 more contract(s)
- variants: none declared
- use instead of: a bare <article> with your own classes, or literal <article data-bn="pkg-card"> markup

## `@basenative/notify`

### `renderEmail(template, data)`
Render an HTML email template with {{ variable }} interpolation.
`packages/notify/src/email.js`
- variants: none declared
- use instead of: hand-written markup for this element

## `@basenative/og-image`

### `renderCard(card, env, opts)`
Build a brand card and rasterize it — the common case, in one call.
`packages/og-image/src/index.js`
- variants: none declared
- use instead of: hand-written markup for this element

### `renderSvg(svg, env, opts)`
Rasterize an SVG document to PNG bytes.
`packages/og-image/src/index.js`
- variants: none declared
- use instead of: hand-written markup for this element

## `@basenative/runtime`

### `renderWithBoundary(renderFn, options)`
Server-side error boundary for render(). Wraps template rendering and returns fallback HTML on error.
`packages/runtime/src/error-boundary.js`
- variants: none declared
- use instead of: hand-written markup for this element

## `@basenative/server`

### `renderToReadableStream(html, ctx, options)`
Renders a template and returns a Web ReadableStream. Deferred sections are appended after the main content as…
`packages/server/src/stream.js`
- variants: none declared
- use instead of: hand-written markup for this element

### `renderToStream(html, ctx, stream, options)`
Streaming SSR renderer for BaseNative.
`packages/server/src/stream.js`
- variants: none declared
- use instead of: hand-written markup for this element

## `@basenative/theme`

### `renderIcon(set, name, options)`
Renders one icon from a set as an `<svg>` string.
`packages/theme/src/icons.js` · emits `<svg>`
- variants: none declared
- use instead of: a bare <svg> with your own classes

## `@basenative/visual-builder`

### `<bn-canvas></bn-canvas>`
`packages/visual-builder/src/bn-canvas.js` · emits `<div>`
- variants: none declared
- use instead of: a hand-rolled <div> reimplementation of <bn-canvas>

### `renderCanvas(canvas, componentMap)`
renderCanvas(canvas, componentMap) - Renders canvas nodes to HTML
`packages/visual-builder/src/renderer.js`
- variants: none declared
- use instead of: hand-written markup for this element

### `renderNode(node, componentMap)`
renderNode(node, componentMap) - Render a single node and its children
`packages/visual-builder/src/renderer.js`
- variants: none declared
- use instead of: hand-written markup for this element

