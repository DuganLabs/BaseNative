{
  "$schema": "https://raw.githubusercontent.com/DuganLabs/basenative/main/packages/doppler/schema.json",
  "secrets": [
    {
      "name": "DATABASE_URL",
      "description": "Connection string for the primary database.",
      "required": true
    },
    {
      "name": "SESSION_SECRET",
      "description": "Random 32+ byte string used to sign session cookies.",
      "required": true
    },
    {
      "name": "SENTRY_DSN",
      "description": "Sentry project DSN. Optional in dev.",
      "required": false
    }
  ],
  "configs": ["dev", "prep", "prod"]
}
