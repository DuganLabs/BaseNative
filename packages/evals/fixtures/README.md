# Reference implementations — hand-authored

Known-good BaseNative templates, written by hand, used to sanity-check the harness
itself: the scoring pipeline must pass a template a human certified as correct.

Same rule as `../prompts/`: **no skill, no subagent, and no model may generate
these at any tier.** If a fixture appears to be missing, report it — do not write one.
